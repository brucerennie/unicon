window.USCRIBE_SEARCH = [{"file":"utr25.html","body":"1. Introduction This report records the as-built native Unicode support in the Unicon runtime: how a string is tagged, how literals and I/O become tagged, which operations interpret positions as codepoints, and what is still byte-oriented. It is formatted as a Unicon Technical Report  .   collects short programs by operation. Automated tests live under  tests/unicode/ . The feature is optional. A 64-bit build without  NoUniconUnicode  reports  Unicode  in  &features . On 32-bit, or with that switch, the tag macros are no-ops and the suite is skipped. 1.1 Terminology: Unicode vs. UTF-8 These are not interchangeable in this work. Unicode  is the language-facing idea: codepoints, text semantics, and the feature itself ( &features  reports  Unicode ;  _UNICODE ;  UniconUnicode ;  unicode() ,  uchar() ,  uord() ;  *  /  s[i]  on a tagged string).  unicode(s)  means \"treat these bytes as Unicode text,\" not \"encode as Unicode.\" UTF-8  is the encoding of the payload  : the bytes in string space,  open   i -mode decoding, lead-byte width scans, and well-formedness. ASCII is a subset of UTF-8; a plain (untagged) string is a byte string.  type()  stays  \"string\"  for both views. The language-level inverse of  unicode(s) , and the difference between a content test and a tag test, are discussed in  . Internal C names ( F_UniQual ,  IsUniQual ) stay as the tag; comments should say \"UTF-8-tagged qualifier.\" The class library  UTF8 / UTF8Set  is correctly named: it is a byte-walking encoding layer. Native support is the Unicode view of those same bytes. 2. Design principle and representation Unicon strings are qualifiers: a two-word descriptor consisting of a length word ( dword ) and a pointer into string space ( vword.sptr ). There is no allocated block and no indirection beyond the pointer itself. This representation is why plain string operations in Unicon are cheap, and it is the property this design is built to preserve: a string containing no non-ASCII bytes must cost exactly what it costs today, in memory layout, in macro behavior, and in execution speed. The approach taken is to reuse bits already present in the descriptor's length word rather than introduce a new block type for Unicode-aware strings. A 64-bit word gives far more range than any realistic string length needs, leaving room to encode additional state in the unused high bits without changing the descriptor's size or a plain string's representation at all. 2.1 Bit layout Available on 64-bit builds ( WordBits == 64 ) when compiled with  UniconUnicode  defined ( src/h/config.h ). With the feature disabled, every macro described below reduces to today's unmodified behavior, so no call site needs its own conditional compilation. bit 63       F_Nqual        (existing -- qualifier vs. typed value) bit 62       F_UniQual      (this string is UTF-8-tagged) bits 32-61   cp_count       (30 bits, cached codepoint count) bits 0-31    byte length    (32 bits, ~4.3 GB maximum) Byte length retains a full 32 bits rather than being reduced further.  Unlike the codepoint count, byte length has no fallback: if a string's true length does not fit in its field, there is no secondary source of truth to recover it from, since Unicon strings are not null-terminated. The codepoint count, by contrast, can always be recomputed by walking the string when it is not cached, so it can safely be given whatever bits remain. In practice this is generous rather than tight -- the sentinel value (see below) is reached only by a string close to the full 4.3 GB byte-length limit that is almost entirely single-byte content, which would not be UTF-8-tagged in the first place under normal conditions. Why the remaining bits were spent on a cached codepoint count rather than a cached scan position.  Two different kinds of cache were considered for these bits. A cached  codepoint count  records how many codepoints a string contains, making the size operation ( *s ) constant-time. A cached  scan position  instead records a single (codepoint index, byte offset) pair -- the last position looked up -- letting a subsequent lookup near that position resume without walking from the start of the string. Both are legitimate optimizations, but they help different things: a cached count benefits every size query uniformly, regardless of string length, since there is no cheaper way to answer \"how many codepoints does this string have\" without either counting or having already counted. A cached position only pays for itself when a string is subscripted repeatedly and nontrivially; for short strings accessed only a few times, walking from the start is already competitive with or faster than maintaining any index, a result obtained by direct measurement rather than assumed. Because a large share of string values in typical Unicon programs are short-lived tokens -- parsed, compared, and discarded, rarely subscripted more than once or twice -- the cached-count approach has the stronger claim on the representation's limited bits, and is what this design uses. A cached-position representation matching the same bit budget was designed and benchmarked in the course of this work (a 14-bit cached codepoint index paired with a 16-bit cached byte offset, packed into the same word) and was found to match a fully allocated index structure to within a few percent for strings up to roughly sixteen thousand codepoints. It was not adopted because the codepoint-count design above has the better-evidenced claim on the same bits, not because it does not work; it remains a candidate for a future representation, discussed in  . 2.2 Core macros Defined in  src/h/rmacros.h : StrLen(q)         /* byte length, masked to exclude tag/count bits */ SetStrLen(q,n)    /* sets byte length; see section 2.3 for why this clears                      rather than preserves the other fields */ IsUniQual(d)      /* tests the UTF-8 tag */ SetUniQual(d)     /* sets the UTF-8 tag */ CpCount(q)        /* reads the cached codepoint count, or the sentinel                      value 0 if none is cached */ SetCpCount(q,n)   /* sets the cached codepoint count */ Three shared helpers implement UTF-8 decoding logic used throughout the runtime: uq_lead_width(b)              /* width in bytes of the UTF-8 character                                  starting at lead byte b */ uq_scan(bytes, len, &ncps)    /* scans a byte range once; returns                                  whether any multi-byte character was                                  found, and the total codepoint count */ uq_seek_cp(bytes, target_cp)  /* returns the byte offset at which                                  codepoint index target_cp begins */ All three treat a malformed lead byte as a one-byte unit and continue, rather than raising an error; this lenient behavior is specific to internal scanning and is distinct from the strict validation applied by  uord()  ( ). 2.3 ``StrLen`` masking and ``SetStrLen`` overwrite semantics StrLen  unconditionally masks out the tag and count bits before returning a byte length. This has to be unconditional rather than applied only where Unicode-awareness is expected, because a tagged string can reach any code path that handles strings generically --  write() , list and table operations, anything treating a string as an opaque byte sequence -- and any of those reading the raw, unmasked word would compute an incorrect length. SetStrLen  performs a full overwrite of the descriptor's length word, rather than clearing only the length bits and preserving whatever tag and count bits were already present. This is a deliberate choice, not an oversight: the large majority of call sites that assign a string length are populating a freshly declared descriptor -- a local variable on the stack, an uninitialized table slot -- whose prior content is unrelated garbage rather than a meaningful previous value. Preserving that content would leak it into the tag and count fields of what is meant to be a clean, newly constructed string. A full overwrite (with the incoming length itself masked, so that an oversized value cannot bleed into the neighboring count field) produces a deterministic, untagged result in every case, which is what nearly every call site actually requires. The consequence is that any code that needs to both set a length and retain or apply tagging must do so explicitly, in order:  SetStrLen , then  SetUniQual , then  SetCpCount  if applicable. Reversing this order silently loses the tag, since  SetStrLen  clears it. This ordering requirement is not enforced by the type system and must be checked by hand at any call site that does both. Concatenation ( ) is the clearest illustration of where getting this wrong produces a real, silent defect: an implementation that computes a combined length via  SetStrLen  and only afterward decides whether to tag the result is correct; one that sets the tag first, relying on an earlier whole-descriptor copy to have carried it, is not, because the subsequent  SetStrLen  call clears whatever the copy carried. 3. Promotion: how a string becomes tagged A string literal's bytes are fixed and known at compile time. Rather than defer classification to when the program runs, the compiler ( icont ) scans a literal once during compilation and encodes the result directly in the compiled program, so that loading and executing the program never needs to re-examine the literal's bytes to determine whether it is Unicode content. Tagging a literal is a compile-time concern only; there is no runtime scan. An earlier iteration of this work included a runtime scan-on-first-use as a fallback for literals compiled by a version of  icont  predating this feature, on the reasoning that it would let such a program benefit from tagging without recompilation. That reasoning turned out not to hold up: the interpreter cannot distinguish \"this literal is untagged because a Unicode-aware compiler determined it is pure ASCII\" from \"this literal is untagged because the compiler that produced it knew nothing about tagging\" -- both look identical, an unset tag bit. A scan keyed on \"not yet tagged\" therefore cannot skip pure-ASCII literals selectively; it must scan every one of them, from every program, on their first execution, to confirm what a Unicode-aware compiler had already determined at compile time. This defeats the purpose of compile-time tagging for the overwhelmingly common case -- a program compiled by a current  icont  -- in order to support the comparatively rare case of running old, unrecompiled icode. Since recompiling with a current  icont  is a normal, low-cost expectation for picking up a new compiler feature, the runtime scan was removed rather than kept as a fallback. A program compiled by a version of  icont  predating this feature runs with every literal's tag bit unset, which is indistinguishable from, and behaves identically to, this feature being entirely absent -- correct, unsurprising behavior requiring no special case, with recompilation being the ordinary path to Unicode-aware literals. 3.1 Compile-time tagging src/icont/tsym.c 's  putlit , which registers each literal encountered during compilation, scans a string literal's already-decoded bytes (the compiler's own literal-decoding stage has already resolved any escape sequences by this point, so no further byte transformation occurs between here and the final compiled output) and sets a flag,  F_UniQualLit , defined in  src/icont/tsym.h  and  src/icont/link.h . This flag occupies the bit immediately above the four existing literal-type flags ( F_IntLit ,  F_RealLit ,  F_StrLit ,  F_CsetLit ); it is unrelated to the runtime's  F_UniQual  bit position, a separate bit space entirely, and the two should not be conflated. The flag is carried through the compiler's intermediate representation using the existing mechanism already used for the other literal-type flags, requiring no format changes. At final code emission ( src/icont/lcode.c 's  uniquallen ), if the flag is set,  F_UniQual  is encoded directly into the length value written into the compiled program. Because this value is written permanently into the compiled program rather than patched in at run time, the interpreter's literal-loading instruction ( Op_Str  in  src/runtime/interp.r ) simply loads it -- including on every re-load performed by that instruction's own pre-existing, unrelated self-patching optimization, which rewrites itself after first use to skip recomputing a literal's address on subsequent executions. No interaction between the two was needed beyond this: the tag is already correct in the bytecode the first time it is read, and remains correct on every later read, without any additional mechanism. One implementation detail is worth recording because it is not obvious from the surrounding code: the values involved in constructing this tagged length are 64 bits wide, while the pre-existing internal representation of a literal's length during compilation is 32 bits ( int ) in two places -- the parameter to the function that emits the final instruction, and the field holding a literal's length in the compiler's symbol table. Writing a 64-bit tagged value through either as originally typed would silently truncate it. The chosen fix widens only the function parameter at the point where the tagged value is actually constructed, leaving the symbol-table field's width and the compiler's intermediate file format untouched; the alternative of widening the symbol-table field would have required corresponding changes to that intermediate file's text-based serialization, a larger and riskier change for no additional benefit. Compile-time tagging encodes the UTF-8 tag but not a cached codepoint count, since doing so would require the same width change that was deliberately avoided above. As a consequence, a compile-time-tagged literal's codepoint count is not cached until something explicitly computes it; the size operation on such a literal remains correct, falling back to a full scan, but does not receive the constant-time benefit that a cached count would provide until a future revision addresses this ( ). 4. ``uchar()`` and ``uord()``: Unicode-aware character conversion Unicon's existing  char()  and  ord()  functions convert between an integer ordinal and a single-character string. Both are strictly byte-oriented:  char(i)  accepts only  0 <= i <= 255  and produces a single-byte string;  ord(s)  accepts only a string of exactly one byte. These functions are unchanged by this work and are expected to remain byte-oriented indefinitely, since existing code depends on that behavior for purposes unrelated to Unicode text -- base-256 byte encodings, and lookup tables indexed by raw byte value among them. Two new functions provide Unicode-aware equivalents: uchar(i)  accepts any Unicode scalar value ( 0 <= i <= 0x10FFFF , excluding the surrogate range  0xD800 - 0xDFFF , which are not valid standalone Unicode scalar values) and returns its UTF-8 encoding as a string, tagged when the encoding is genuinely multi-byte. uord(s)  accepts a string and returns its codepoint value, requiring that  s  consist of exactly one well-formed UTF-8 encoded codepoint -- correct length for its lead byte, correctly formed continuation bytes, no overlong encoding, and no surrogate value. Unlike the lenient byte-width detection used internally for scanning ( ),  uord()  is strict: malformed or wrong-length input raises a runtime error rather than failing silently, matching the convention already used by  ord()  for its own precondition violations. Both are registered as ordinary built-in functions ( src/h/fdefs.h ) and implemented in  src/runtime/fmisc.r  alongside  char()  and  ord() . Round-trip correctness ( uord(uchar(i)) = i ) has been verified across both the Basic Multilingual Plane and the supplementary planes, including four-byte-encoded codepoints. 5. Concatenation The  ||  operator ( src/runtime/ocat.r ) has three internal implementation paths, corresponding to different memory-layout optimizations (zero-copy when operands are already adjacent in string space, in-place extension when the left operand is the most recently allocated string, and a general copying path otherwise). All three propagate both the UTF-8 tag and the cached codepoint count to their result: the tag is set if either operand is tagged; the codepoint count is computed as the sum of each operand's contribution, where an untagged operand's own byte length stands in for its codepoint count (valid, since an untagged string is by construction pure ASCII), and a tagged operand contributes its cached count if one is available. If either operand's count is not available (uncached or otherwise unknown), the result's count is left uncached rather than guessed, and a subsequent size query on the result falls back to a full scan. Per  , all three paths must apply tagging after computing the result length via  SetStrLen , not before. 6. Deliberately unscanned input: files and sockets Data arriving through  read() ,  reads() , and equivalent socket operations is not scanned or tagged automatically, regardless of its actual content. This is a deliberate design decision rather than an oversight, made after comparing a byte-copy-only implementation against one that also scans for non-ASCII content in the same pass. Read operations, unlike literals, have no repeated-execution structure to amortize a scan's cost against: every call processes genuinely new bytes, so a mandatory scan would impose its cost on every call to every program performing I/O, whether or not the data ever contains non-ASCII content. Measured in isolation, a combined copy-and-scan implementation was found to run roughly sixteen times slower than a copy-only implementation at a typical line-read size (128 bytes), and remained an order of magnitude or more slower across read sizes from tens of bytes to several kilobytes, since a modern  memcpy  implementation is substantially better optimized than an equivalent scanning loop. Given the principle that ASCII content should not pay a cost, automatic scanning of read data was rejected. This has one consequence worth stating precisely, since it affects the correctness reasoning in  : an untagged string is assumed to be pure ASCII by every operation that examines the tag, including concatenation's codepoint-count propagation. That assumption holds for every string this implementation produces internally, but does not hold in general for content read from an external source that happens to contain multi-byte UTF-8 and was never scanned. A program that reads such content and relies on native Unicode-aware operations without first tagging it explicitly may see byte-oriented rather than codepoint-oriented results -- correct in the sense that no data is lost or corrupted, but not Unicode-aware until the content is tagged by one of the mechanisms below. Three mechanisms give a program control over tagging where it is needed, rather than making it automatic: unicode(s)  ( src/runtime/omisc.r ), an explicit, on-demand function. If  s  is untagged, it is scanned; if the scan finds non-ASCII content, the result is tagged and its codepoint count cached. If  s  is already tagged but has no cached count (the common case for a compile-time-tagged literal,  ), the count is computed and cached. If  s  is pure ASCII, it is returned unchanged.  string(s)  is not the inverse; see  . The  i  mode character to  open() , which sets a per-file or per-connection flag causing subsequent  reads()  calls on that file or connection to scan and tag their results automatically. This is implemented at all of the function's internal return points that produce a taggable result -- the file, socket, and secure-shell channel paths -- with the exception of pseudo-terminal reads, which return through a different internal mechanism not yet adapted for tagging ( ). A global default , changing what  open()  assumes when a caller does not specify a mode explicitly, has been designed but not implemented. Adding a new file-status flag to  open() 's mode-character parsing is not sufficient by itself to make the flag usable on every connection type: socket and messaging connections are each additionally validated against an explicit allowlist of permitted flag combinations ( src/runtime/fsys.r ,  if (status & ~(...)) runerr(...) ), independent of the character-parsing switch, and a new flag must be added to each relevant allowlist as well or it will be silently rejected for that connection type despite being accepted by  open() 's general mode parsing. 7. Language API: views, conversion, and tests type()  is  \"string\"  for both views. No  \"unicode\"  type:  type(x) == \"string\"  and  ||  /  string()  treat tagged values as strings. ASCII is never tagged.  unicode(\"hello\")  is a no-op. On ASCII,  *s  /  s[i]  / scanning already match the text view. unicode(s) : text view ( *  /  []  are codepoints). Inverse of  unicode(s) : same UTF-8 bytes, untagged descriptor ( *  /  []  are bytes). Not implemented. Name open. Implementation is a descriptor copy plus  SetStrLen . Current untag path:  writes  to a file,  reads  without  i . Not an API. 7.1 ``string()`` is not the inverse string(x) : convert to string or fail. Identity on strings, including the tag.  string(a, b, ...)  concatenates and keeps the tag ( ||  does the same). Must stay identity:  s := string(x) | fail  is the usual coerce. Used in  uni/lib/format.icn , GUI text fields, XML  string(e) , IPL. Stripping the tag would make later  *  /  []  byte-oriented on  \"café\" . if string(x) then  still succeeds as a type test if the tag is dropped;  if s := string(x) then  would bind the byte view. Rejected inverse names:  bytes(s)  (storage size),  utf8(s)  (encode; opposite of  unicode(s) ),  plain(s) . No builtin  isunicode(s)  if the inverse exists; see  . 7.2 Content test vs. tag test Two different questions.  foo  is the inverse of  unicode . Question isunicode (content) istagged (descriptor) Asks text view ~= byte view? is  F_UniQual  set on this value? Form *unicode(s) ~= *foo(s) must read the bit; no derived form *unicode(s) codepoints (bytes if ASCII) *foo(s) always bytes s *unicode(s) *foo(s) content  ~= tagged? \"hello\" 5 5 no no \"café\"  (literal) 4 5 yes yes café bytes, no  i 4 5 yes no The last two rows have the same  *  comparison and a different tag. A content test is not a tag test. Inverse plus  *  covers content. A tag predicate is needed only if a program must ask \"am I in the text view?\" without assigning  unicode(s) . 8. Examples These examples assume a Unicon build with  Unicode  in  &features . Non-ASCII literals are tagged at compile time, so  \"café\"  already has a text view:  *s  and  s[i]  are codepoints. The programs under  tests/unicode/  are the executable form of this section;  languages.icn  is a longer script sample. 8.1 Length and subscript *s  is 4, not 5.  s[4]  is the letter é, not a continuation byte: procedure main()    local s, i    s := \"café\"    write(\"*s = \", *s)    every i := 1 to *s do       write(\"s[\", i, \"] = \", s[i]) end Output: *s = 4 s[1] = c s[2] = a s[3] = f s[4] = é \"hello\"  is untagged ASCII, so  *  and  []  still mean bytes, which for ASCII are the same as codepoints. 8.2  unicode() ,  uchar() , and  uord()  --------------------------------------------- unicode(s)  tags non-ASCII UTF-8.  uchar  /  uord  are the codepoint pair;  char  /  ord  stay byte-oriented ( ): procedure main()    write(*unicode(\"hello\"))    write(*unicode(\"café\"))    write(unicode(\"café\")[4])    write(uord(uchar(233)))    write(*uchar(233))    write(char(65), \" \", ord(\"A\")) end Output: 5 4 é 233 1 A 65 uord  on more than one codepoint is error 205. 8.3 Concatenation ||  tags the result if either operand is tagged, and adds cached codepoint counts when both are known ( ): procedure main()    write(*(\"café\" || \"naïve\"))    write(*(\"café\" || \"hello\"))    write(*(\"hello\" || \"café\")) end Output: 9 9 9 string(a, b, ...)  concatenates too and  keeps  the tag, the same as  || . It is not a byte-view switch ( ). 8.4 Scanning On a tagged subject,  move ,  tab , and  pos  use codepoint positions: procedure main()    &subject := \"café naïve\"    &pos := 1    write(move(4))    &pos := 1    write(tab(5))    &pos := 1    write(pos(1)) end Output: café café 1 8.5 ``open()`` mode ``i`` Without  i ,  reads()  is untagged (byte  * ). With  i , the same UTF-8 is tagged ( ): procedure main()    local f, s    f := open(\"uio.dat\", \"wu\") | stop(\"open\")    writes(f, \"café\")    close(f)    f := open(\"uio.dat\", \"r\") | stop(\"open\")    s := reads(f, 100)    close(f)    write(*s)    f := open(\"uio.dat\", \"ri\") | stop(\"open\")    s := reads(f, 100)    close(f)    write(*s)    write(s[4])    remove(\"uio.dat\") end Output: 5 4 é 8.6 Content vs. tag Untagged UTF-8 from a file, or built from  char()  bytes, has byte  * .  unicode()  is the text view. The two disagree exactly when the payload is multi-byte ( ): procedure main()    local raw    raw := char(195) || char(169)    # UTF-8 of é, untagged    write(*raw)    write(*unicode(raw))    write(*string(unicode(raw))) end Output: 2 1 1 string(s)  on  \"café\"  also keeps the tag:  *string(s) = *s . 9. Status and future work The following operations have been made Unicode-aware. Subscripting ( s[i] ) and section extraction ( s[i:j] ) both include the case of extraction from a named variable, which for either operator is handled by a separate code path ( src/runtime/cnv.r 's dereferencing of a trapped substring variable) rather than by the operator's own logic directly, and independently applies the same tagging rule -- the result is tagged only when the extracted range is genuinely multi-byte, not simply because the source was tagged, since a slice of a tagged string can itself be pure ASCII. Element generation ( !s ) and random selection ( ?s ) use the same codepoint unit on a tagged string: each result is one complete UTF-8 character, matching  s[i] , rather than a single byte that could split a multi-byte encoding. Substring assignment ( s[i] := x  and the range form  s[i:j] := x ) shares this same trapped-variable mechanism on assignment and is complete as well; assigning into a tagged string correctly recomputes the result's tag and codepoint count by scanning the assembled result rather than assuming either the prefix or the suffix inherits the source's tag.  move ,  tab , and  pos , which operate on  &subject  and  &pos , have been made to interpret their positions as codepoints for a tagged subject wherever the underlying position arithmetic ( cvpos ) already treats units abstractly; only the point where an abstract position becomes an actual byte offset needed a tagged-aware branch.  many , among the pattern-scanning functions, has been made Unicode-aware in one specific respect: it walks a tagged subject codepoint by codepoint rather than byte by byte, so that its notion of position is codepoint-based. It does not, and cannot yet, test a multi-byte codepoint against the character set argument at all -- a plain character set represents only codepoints 0-255, so there is no meaningful membership test to perform for one. The implementation reflects this directly rather than approximating it: on reaching a multi-byte codepoint, it stops immediately, without ever calling the character-set membership test on any of that codepoint's bytes. This was verified deliberately, not assumed -- including a check that a character set constructed to contain the exact raw byte values of a multi-byte codepoint's UTF-8 encoding still does not cause the scan to continue through it, ruling out the specific failure mode of accidentally testing individual continuation bytes as if they were independent ASCII characters. Extending  many  (and the character-set type more generally) to test Unicode content meaningfully requires a codepoint-range set type, which does not exist yet (below).  find  and  match , which search for and test one string against another rather than testing a character set, have also been made Unicode-aware: the byte-level comparison each performs internally is unchanged and does not itself need to become codepoint-aware, since UTF-8 is self-synchronizing -- an exact byte match beginning and ending at codepoint boundaries is already a correct codepoint-level match. Only the bounds each accepts and the position each returns needed to change, to mean codepoints rather than bytes for a tagged subject;  find , as a generator, walks one codepoint at a time across its search window for this reason, testing the byte comparison at each codepoint boundary rather than at each byte offset. The size operator ( *s ), concatenation ( || ), and the explicit  unicode() ,  uchar() , and  uord()  functions are also complete, as described above. Table and set key equality, lookup, and membership testing are unaffected by tagging, confirmed by direct test rather than assumed from the general masking behavior described in  : a tagged literal, a byte-identical untagged string read from a file, and a string explicitly tagged via  unicode()  all compare equal to one another and are interchangeable as table keys and set members, since hashing and equality both operate on  StrLen -masked byte content only. reverse  has also been made Unicode-aware, and needed a different fix than the position-based functions above rather than a lighter version of the same one: a naive byte-for-byte reversal, which is what the original byte-oriented implementation did and is a correct way to reverse an ASCII string, actively corrupts multi-byte content, since reversing byte order breaks the lead-byte/continuation-byte structure a multi-byte encoding depends on. This was confirmed as real before being fixed, not assumed: reversing a string containing one two-byte character produced a byte sequence a UTF-8 decoder cannot correctly interpret. The fix reverses codepoint order while leaving each codepoint's own bytes in their original internal order, by walking the source forward one codepoint at a time and writing each into the result working backward from the end, so the first codepoint encountered ends up last as a unit rather than having its own bytes individually reversed. Round-trip correctness ( reverse(reverse(s)) = s , byte for byte) and correct behavior for a string containing several multi-byte codepoints have both been verified. The following remain byte-oriented and unfixed.  bal ,  any , and  upto  share  many 's original, unfixed shape -- testing a single position or byte-based run against a character set -- and each was confirmed, by direct test rather than by inference from the others, to report byte positions rather than codepoint positions for tagged content: a character immediately following a multi-byte codepoint is reported at its byte offset, one or more past its true codepoint index. For  any  specifically, this was traced further, to the level of individual bytes: testing a position that falls on a multi-byte codepoint's lead or continuation byte tests that isolated byte against the character set independently, rather than recognizing that no plain character set can represent the codepoint as a member at all.  detab ,  entab ,  map ,  trim , and  sort / sortf , by contrast, were confirmed safe despite being byte-oriented: none of them reorder or split an existing multi-byte byte sequence, so a tagged string's encoding survives each of them intact even though position, padding, or width calculations remain byte-based rather than codepoint-based.  center ,  left , and  right  fall in this same safe-but-imprecise category. Fixing  bal  and  any  ( upto  and  map  also, since both take a character-set argument) is blocked on the same codepoint-range character-set type as  many . Long strings under repeated access.  The current implementation performs subscripting on a tagged string by walking from the nearer end of the string on every access; no index or position cache is maintained. This is appropriate for the dominant case in real Unicon programs -- short-lived string values accessed a small number of times -- but scales poorly for a long string accessed repeatedly, such as a parser scanning across a large source file. Controlled, isolated benchmarking (independent of the runtime, measuring the underlying walk-versus-index algorithms directly rather than the interpreter) found the following, for a 100,000-codepoint string with mixed single- and multi-byte content, comparing the no-cache walk against an indexed scheme combining a single-entry position cache with a bucketed index: access pattern walk (no cache) indexed ratio bursty, front-biased (typical tokenizer access) 41.9 us 0.070 us ~600x sequential scan (typical tokenizing walk) 245.4 us 0.133 us ~1,850x A block-based representation with an optional index was designed and benchmarked to address this -- an allocated structure (tentatively  struct b_unistr ) carrying a single-entry position cache together with an optional bucketed index, with a default bucket size of 8 determined by sweeping the tradeoff between index memory overhead and lookup speed -- but was not implemented. The open design question is not the structure itself but the trigger: when a tagged string should be promoted into this representation, and, once promoted, when its index should be upgraded from the cheap single-entry cache to the full bucketed form. Neither threshold was resolved; a length-based threshold was tested directly and found not to work; the more promising direction identified was triggering the upgrade based on observed access cost during actual use rather than on any property known at the time a string is first promoted. A related possibility, not yet implemented, is an explicit optional argument to  unicode()  allowing a program that already knows its own access pattern to request the indexed representation proactively, bypassing the need for an automatic trigger. A language-level inverse of  unicode()  is still unnamed ( ). The operation is settled (same bytes, untagged descriptor).  string()  is not that operation. A content test can be derived from the inverse via  * ; a tag test cannot. icont  caching a codepoint count, not only the tag, for literals  ( ) would remove the one remaining asymmetry between compile-time and runtime tagging, at the cost of the same intermediate-file width consideration that was avoided when implementing the tag itself. Codepoint-range character sets.  Unicon's existing cset type is a 256-bit membership table and cannot represent codepoints beyond 255. A design extending csets to represent arbitrary codepoint ranges, using a sorted range list for codepoints above 255 while leaving the existing bitmap representation untouched for 0-255, was evaluated against alternative representations and found to use substantially less memory across a range of real Unicode block compositions, but was not implemented.  many  (above) is a concrete existing example of what this blocks: it correctly walks a tagged subject by codepoint, but has no character-set representation capable of matching a non-ASCII codepoint against, regardless. Pseudo-terminal reads  do not currently support the  i  mode mechanism described in  ; the internal function handling them constructs its return value through a different mechanism than the paths that have been adapted, requiring restructuring rather than a direct extension of the existing approach. Existing UTF-8 support in the standard library.   uni/lib/utf8.icn   , a pre-existing class-based UTF-8 implementation predating this work, manages UTF-8 content by walking strings byte-by-byte under the assumption that string indexing is always byte-oriented -- a correct assumption before this feature existed, and no longer a safe one for a tagged string, since indexing on a tagged string is codepoint-oriented. The library's tests are consequently excluded from the test suite when this feature is enabled ( tests/general/Makefile , conditioned on the  unicode  feature being present in  &features ). The library continues to provide functionality with no native equivalent yet -- codepoint-range set operations and several scanning primitives -- and remains usable by programs that do not enable native tagging; reconciling the two, either by adapting the library to native tagging or by extending native support to cover what it currently provides, is unresolved. Appendix: test coverage Automated tests specific to this feature live under  tests/unicode/ , one program per concern ( size ,  subsc ,  concat ,  convert ,  scan ,  section ,  bang ,  many ,  match ,  find ,  reverse ,  file_i ,  socket_i , plus  ascii_*  and  languages ).  tests/general/Makefile  excludes the pre-existing  utf8 / utf8_ovld  tests ( ) when the feature is enabled. References ","title":"UTR 25: Native Unicode Support in the Unicon Runtime"},{"file":"utr26.html","body":"1. Introduction This report describes the current design and implementation of native SSH in the Unicon runtime: how a session is opened, how additional channels and SFTP handles are derived from it, how stream I/O, ordered events, and metadata operations are exposed, and what remains out of scope. It is formatted as a Unicon Technical Report  . The language-facing reference lives in  Programming with Unicon  (Chapter 6, \"Secure Shell\") and the language reference ( open  mode  h ,  Attrib ,  receive ,  stat  /  remove  /  rename ).   collects examples by mode and use case. Automated tests live in  tests/posix/ssh.icn . The feature is optional. A build with libssh present reports  secure shell  in  &features  (the  _SSH  feature flag). Without the library, mode  h  is ignored and the SSH paths are compiled out, as with other optional features. 2. Motivation and prior art Unicon programs that needed SSH previously relied on  popen()  or pipes to an external  ssh  process. That is not portable (Windows often has no OpenSSH client in  PATH ) and gives the program no structured access to channels, exit status, or SFTP. Native primitives let an application authenticate to hosts, run commands, and transfer files without an external process. 2.1 Library choice Two C libraries were considered.  libssh  (LGPL) supports both client and server, modern key exchange and host keys, and true non-blocking I/O via  poll(2) .  libssh2  (BSD) is client-only and cannot be used fully non-blocking. The implementation binds  libssh   , for server-side optionality later and for the callback API that preserves stdout/stderr arrival order ( ). A full protocol reimplementation (Paramiko, Go's  x/crypto/ssh ) would mean owning crypto primitives and protocol-drift / CVE tracking indefinitely. Binding to libssh via the existing C runtime avoids that. 2.2 Open as the verb Python (Paramiko) and Go ( x/crypto/ssh ) both treat \"new session\" as sugar over \"open another channel on this connection.\" The verb is  open , applied to an existing connection object, not \"clone\" or \"duplicate.\" Unicon already has  open()  as the universal constructor for files, sockets, SSL, and messaging connections.  Clone()  is the Graphics-facility naming convention; a distinct  channel()  function would add a new global name for an operation  open()  already expresses. 2.3 Why not Messaging The Messaging facility (HTTP/POP/SMTP,  rmsg.r , libtp)   is not a fit. Messaging's transport-discipline abstraction has the same 1:1 single-stream limitation as SSL -- no channel-multiplexing concept -- so moving into that world would not solve the multiplexed-channel requirement, only relocate it. SSH's request/channel model ( exec  /  shell  /  sftp  as concurrent channels) also does not map onto Messaging's request/response verb model ( GET  /  POST  /  RETR ). SSH does not route through  ssh://  URIs or  Mopen() . It uses the  h  mode character and plain  user@host  string parsing, independent of the Messaging/URI subsystem. 3. Architectural grounding The implementation follows established SSL and socket patterns rather than inventing a parallel subsystem. union f  in  src/h/rstructs.h  is the file block's descriptor slot: a tagged union of  FILE * , socket  fd ,  SSL *  when  HAVE_LIBSSL , and so on, discriminated by status bits at runtime. SSH does  not  embed libssh objects directly in the union. It adds a pointer,  struct SSHfile *sshf , matching the Messaging  MFile *  precedent.  SSHfile  is allocated with  malloc  so pointers to it are stable across garbage collection, and is freed by the close hook, never by the collector. A malloc'd block is required once the session must hold a list of child channels whose addresses survive compaction ( ). Status bits live in  src/h/rmacros.h .  Fs_Encrypt  is  0200000000 ; SSH takes the next bit,  Fs_SSH = 0400000000 .  open() 's mode string is scanned character-by-character in  src/runtime/fsys.r .  e  sets  Fs_Encrypt . SSH uses  h  /  H  ( s  is already the Messaging short-request flag). Attributes are  \"key=value\"  strings passed as trailing arguments to  open() , parsed in  create_ssh_session()  the same way  create_ssl_context()  parses SSL attributes -- not a fixed argument list. A leading integer, when present, is a connect timeout in milliseconds, as for plain sockets. All stream I/O funnels through one dispatch point:  u_read()  /  u_write()  in  src/runtime/rposix.r , which already branch on  Fs_Socket  /  Fs_Encrypt . SSH adds a third branch for  Fs_SSH , reading from the arrival-order queue ( ) or  sftp_read() , and writing via  ssh_channel_write()  or  sftp_write() . Nothing above this dispatch ( reads() ,  read() , and so on) needs its own SSH case, except where a line-oriented helper must not wait for a newline that will never arrive ( ). Close is  explicit, not GC-driven . The close hook in  fsys.r  frees SSL resources directly; there is no finalizer during garbage collection. SSH files follow the same convention -- leaking a channel or session means not calling  close() , the same as any other file. receive()  already returns a general-purpose  posix_message  record (fields  addr  and  msg ) for UDP and raw datagrams. SSH reuses that type for an ordered stdout/stderr/exit-status event stream ( ). stat(f)  already  type_case s on its argument (string path versus open file). SFTP extends both branches ( ).  remove()  and  rename()  gain an optional leading session argument, matching the existing Unicon idiom that many functions take an optional leading file or window ( write(\"hello\")  versus  write(f, \"hello\") ). 4. Connection and channel model 4.1 ``open()`` dispatch Two shapes, both using the existing  open() . The second argument is the usual mode string;  h  is a new character in that scan, and  c  /  s  after  h  select a channel or SFTP the same way  u  after  n  selects UDP ( n  is TCP,  nu  is UDP). open(host_or_\"user@host\", \"h...\", attrs...)  -- first argument is a string: opens a new authenticated SSH  session  and, by default, an interactive shell channel on it ( ). open(s, \"hc\"/\"hs\", attrs...)  -- first argument is an existing SSH session or channel file: opens a  new channel  ( hc ) or SFTP handle ( hs ) on the same underlying session (reusing the transport and authentication, no new handshake). Opening on a channel opens a  sibling  on the session owner, not a child of the channel. A session opened with  channel=no  has no channel of its own ( Fs_SSH  alone, no  Fs_Socket ). It exists so later  open(s, \"hc\")  /  open(s, \"hs\")  calls have an authenticated transport.  channel=yes  is the default. Combining  channel=no  with mode  hc  or  cmd=  is contradictory and fails with a bad-attribute error. s := open(\"jafar@\" || host, \"h\", \"key=\" || keypath) write(s, \"show version\n\") while line := reads(s) do write(line) close(s) s := open(\"user@host\", \"h\", \"key=id_ed25519\", \"channel=no\") c1 := open(s, \"hc\", \"cmd=uname -a\") c2 := open(s, \"hc\", \"cmd=uptime\") 4.2 Host string The first argument is  host ,  user@host , or either with  :port . The port uses the same  host:port  form as socket  open() ; IPv6 uses  [addr]:port . A bare IPv6 literal is not misparsed: an unbracketed host is split on  :  only when there is a single colon. There is no  port=  attribute. user@host  wins over a  user=  attribute. If neither is given, libssh's default user (the local login name) is used. ssh://user@host  URI syntax is not used ( ). 4.3 Default channel: shell, not bare session open(host, \"h\", ...)  with no  cmd=  and without  channel=no  opens a shell on a remote PTY immediately, so the common interactive case is one handle. The PTY is requested with  TERM  (from  term= , else  $TERM , else  xterm ) and a window size (from  cols=  /  rows= , else the local tty via  TIOCGWINSZ , else 80x24). Without a real size, remote  ls(1)  and friends fall back to one column. A shell channel has no clean command boundaries: prompt text, echoed input, and output all interleave. For scripted use, prefer mode  hc  with  cmd= , which has clean stdout and a real exit status: c := open(s, \"hc\", \"cmd=show version\") 5. Attributes and authentication Trailing  open()  arguments are  name=value  strings. Empty values and unknown names fail (error 1321). Boolean-style attributes without a value are not accepted -- every attribute must contain  = . SFTP is selected by mode character  s  after  h  ( \"hs\" ), not by a valueless attribute. Attribute Meaning Notes user= Remote login name Overridden by  user@host key= Path to a private key file Same convention as SSL's  key= keypass= Passphrase for an encrypted private key Distinct from  password= password= Remote login password hostkeyfile= Path to an OpenSSH  known_hosts  file Passed to libssh as  SSH_OPTIONS_KNOWNHOSTS cmd= Command for a one-shot exec channel Requires mode  hc ; contradicts  channel=no channel= yes  (default) or  no no  is transport-only term= Remote  TERM  for a PTY Default  $TERM  or  xterm cols=  /  rows= PTY size Default from the local tty, else 80x24 keypass=  is deliberately distinct from  password= . SSL's existing  password  attribute means \"key-decryption passphrase,\" a different concept from an SSH login password. Reusing  password=  for both would confuse the two. The  -  mode-character flag disables host-key verification, reusing the existing SSL peer-verification-disable flag ( do_verify ).  Fs_Encrypt  and  Fs_SSH  are mutually exclusive contexts, so there is no conflict. Mode  \"h-\"  is the usual spelling. A leading integer attribute is a connect timeout in milliseconds and is applied as libssh's  SSH_OPTIONS_TIMEOUT  /  TIMEOUT_USEC . The default port is 22 when  :port  is omitted. 5.1 Authentication order If both  key=  and  password=  are given, they are tried in the order the attributes appear in the call, not in a hardcoded priority. If  neither  is given, the implementation falls back to  ssh_userauth_publickey_auto()  -- default identities under  ~/.ssh  and a running agent -- not to password authentication. Agent use through this fallback is supported; an explicit  agent=  attribute and keyboard-interactive auth remain out of scope. key=  does not expand  ~ . Callers (and the  ussh  demo) must expand a leading  ~/  themselves; libssh treats the path literally. 6. Examples and use cases These examples assume a Unicon build with the  secure shell  feature. Connection failures fail with  &errortext  set; the usual form is  s := open(...) | stop(&errortext) . The packaged demo  uni/progs/ussh.icn  is a small client that covers the interactive and one-shot command cases. 6.1 Modes Like every other  open() , SSH uses the second argument as a mode string (it defaults to  \"r\"  when omitted, which is not useful for SSH).  h  /  H  is a mode character in that same scan, alongside  r  /  w  /  n  /  e  /  m . After  h ,  c  selects a command/channel and  s  selects SFTP -- the same \"facility letter, then modifier\" order as  nu  (UDP) and  ms  (Messaging short-request).  n  alone is TCP. Without a preceding  h ,  c  is still create+write and  s  is still ignored (or Messaging short-request after  m ). Trailing  \"name=value\"  arguments are attributes, not modes -- the same slot SSL uses for  key=  and friends. Mode characters combine.  \"h-\"  is SSH plus skip host-key verification (the  -  flag also disables SSL peer checks).  \"hc\"  is a channel (exec when  cmd=  is given, else a shell).  \"hs\"  is SFTP;  r  /  w  /  a  /  b  set transfer intent ( \"hsw\" , or  open(..., \"hs\", ..., \"w\") ). Form Kind Handle open(host, \"h\", ...) mode  h New session plus a PTY shell open(host, \"h-\", ...) mode  h  and  - Same, no host-key check open(host, \"hc\", \"cmd=...\") mode  hc , attribute  cmd= New session plus an exec channel open(host, \"h\", \"channel=no\") mode  h , attribute  channel= Session only (no channel) open(s, \"hc\") mode  hc  on an existing SSH file Extra shell channel on that session open(s, \"hc\", \"cmd=...\") mode  hc , attribute  cmd= Extra exec channel on that session open(s, \"hs\", \"path=...\", \"w\") mode  hs  plus access chars SFTP file or directory open(host, \"hs\", \"path=...\", \"w\") mode  hs  on a host string New session plus SFTP (one handle) leading integer attribute (timeout) Connect timeout in milliseconds channel=no  with  hc  or  cmd=  fails (error 1321).  cmd=  without  c  also fails. 6.2 Interactive shell With no  cmd= ,  open(..., \"h\")  requests a remote PTY and shell.  net::ssh_interactive(s)  attaches the local tty (raw mode,  select()  with  &input , CRLF for display): import net procedure main()    s := open(\"user@host\", \"h\", \"key=/home/user/.ssh/id_ed25519\") |         stop(&errortext)    ssh_interactive(s)    close(s) end term= ,  cols= , and  rows=  override the PTY. Mode  \"h-\"  skips  known_hosts  (useful on a first connect before the key is installed). Password auth uses  password=  instead of, or in addition to,  key= . 6.3 Remote command Mode  \"hc\"  with  cmd=  runs one command and returns its stdout through ordinary  read()  /  reads() .  Attrib(c, \"exitstatus\")  is the remote exit code once it has arrived: procedure main()    s := open(\"user@host\", \"hc\", \"key=/home/user/.ssh/id_ed25519\",              \"cmd=uname -a\") | stop(&errortext)    while write(read(s))    write(\"exit status: \", Attrib(s, \"exitstatus\"))    close(s) end The host string may include a port ( user@host:2222 ) or an IPv6 address ( user@[2001:db8::1]:22 ). A leading integer is a connect timeout in milliseconds: s := open(\"user@host\", \"hc\", 5000, \"cmd=true\") | stop(&errortext) 6.4 Ordered stdout and stderr Stream  reads()  see stdout only. When stdout and stderr must stay in arrival order, or when stderr should go to  &errout , use  receive() : s := open(\"user@host\", \"hc\", \"key=/home/user/.ssh/id_ed25519\",           \"cmd=echo out; echo err 1>&2; exit 3\") | stop(&errortext) while m := receive(s) do    case m.addr of {       \"stdout\" : writes(&output, m.msg)       \"stderr\" : writes(&errout, m.msg)       \"exit\"   : write(&errout, \"[exit \", trim(m.msg), \"]\")       } close(s) Attrib(c, \"stderr\")  is the other stderr path: it drains bytes accumulated since the previous call, without preserving order against stdout. See  . 6.5 Several channels on one session channel=no  opens an authenticated transport. Later  open(s, \"hc\")  /  open(s, \"hs\")  calls add exec, shell, or SFTP handles on that session. Closing  s  invalidates every derived handle: s := open(\"user@host\", \"h\", \"key=/home/user/.ssh/id_ed25519\",           \"channel=no\") | stop(&errortext) c1 := open(s, \"hc\", \"cmd=uname -a\") | stop(&errortext) c2 := open(s, \"hc\", \"cmd=uptime\") | stop(&errortext) while write(read(c1)) while write(read(c2)) close(c1) close(c2) close(s) Opening on an existing channel opens a sibling on the session owner, not a child of that channel. 6.6 SFTP The same session carries SFTP. Mode  \"hs\"  plus  path=  opens a remote file or, if the path is a directory, lists it.  stat ,  remove , and  rename  take the session as a leading argument: s := open(\"user@host\", \"h\", \"key=/home/user/.ssh/id_ed25519\",           \"channel=no\") | stop(&errortext) c := open(s, \"hs\", \"path=/tmp/out.bin\", \"w\") | stop(&errortext) writes(c, ReadBytes(\"local.bin\")) close(c) info := stat(s, \"/tmp/out.bin\") | stop(&errortext) write(\"remote size: \", info.size) d := open(s, \"hs\", \"path=/tmp\") | stop(&errortext) while write(read(d)) close(d) rename(s, \"/tmp/out.bin\", \"/tmp/out.bin.bak\") remove(s, \"/tmp/out.bin.bak\") close(s) 6.7 Concurrent hosts Each connection is blocking. Concurrent hosts use Unicon  thread , one thread per in-flight session: procedure run_cmd(host, key)    local s, line    s := open(host, \"hc\", \"key=\" || key, \"cmd=uname -a\") | {       write(&errout, host, \": \", &errortext)       fail       }    while line := read(s) do write(host, \": \", line)    close(s) end procedure main()    local host, key    key := \"/home/user/.ssh/id_ed25519\"    every host := \"user@alpha\" | \"user@beta\" | \"user@gamma\" do       thread run_cmd(host, key) end Channels that share a session serialize on the session mutex ( ). Separate sessions do not. 7. Error handling Connection and channel failures  fail , they do not  runerr . This matches the SSL connect path: set  &errortext , tear down what was allocated, and fail, so callers write  if s := open(...) then ... else ... . Runtime errors are reserved for type mistakes (passing a non-SSH file to  open(s, \"hc\") , a bad  Attrib  name on an SSH file). Error numbers, defined in  src/runtime/data.r : Number Text Typical cause 1320 SSH error Connect failure, allocation, bad host string 1321 bad ssh attribute Unknown name, empty value,  cmd=  without  c ,  channel=no  with  hc 1322 SSH authentication error No method succeeded 1323 SSH host key verification error Server not in  known_hosts  (when verifying) 1324 SSH channel error Channel open, I/O, or a closed/cascaded handle 1325 SFTP error SFTP subsystem, path, or transfer failure set_ssh_errortext()  appends libssh's own message where available. Blocking libssh calls run under  DEC_NARTHREADS ;  &errortext  is set only after the thread is re-registered, because Unicon allocation is not allowed while unregistered. 8. I/O: streams, ``Attrib()``, and ``receive()`` Three tiers, layered simple-default to full-fidelity. All three share one per-channel event queue populated by libssh callbacks ( ). 8.1 Plain stream I/O read()  /  write()  /  reads()  /  writes()  work unmodified via the existing  u_read()  /  u_write()  dispatch, extended with an  Fs_SSH  branch. Stream reads consume  stdout only . They are enough when the caller does not need to distinguish stderr or preserve cross-stream order. Writes to a channel call  ssh_channel_write()  and retry short writes. Writes to a transport-only session (no channel) fail with SFTP regular files use  sftp_write()  on the same path. 8.2 ``Attrib()`` for exit status, stderr, and EOF Attrib()  already exists generically -- documented as \"read/write attributes (threads, sockets, ...)\" -- and is already used for socket options. SSH channels add three query-only names: Attrib(c, \"exitstatus\")  -- the remote command's exit status, as an integer.  Fails until the exit-status callback has fired.  The implementation pumps the channel until that happens or the channel is done with no status. Preferring callback state over libssh's blocking  get_exit_*  helpers avoids version-sensitive fallbacks once callbacks are registered. This reuses the ordinary Unicon \"fail = not ready\" idiom, the same as  read()  failing at EOF. Attrib(c, \"stderr\")  -- drains  only new bytes since the last call  (a live incremental accessor, not a cumulative snapshot). Not gated on EOF: reading partial stderr mid-run is safe and useful. Returns a possibly empty string. Attrib(c, \"eof\")  --  1  once the remote sent EOF, else  0 . Useful when a caller is multiplexing with  select()  and needs to distinguish \"no stdout yet\" from \"channel finished.\" Unknown names on an SSH file raise 1321. c := open(s, \"hc\", \"cmd=apply-config /tmp/newconfig.json\") while line := reads(c) do process(line) if Attrib(c, \"exitstatus\") = 0 then write(\"ok\") else write(\"failed: \" || Attrib(c, \"stderr\")) 8.3 ``receive()`` for ordered events A command can produce interleaved stdout and stderr where the caller needs to know  when  an stderr message occurred relative to stdout. Neither  reads()  (stdout only) nor  Attrib(c, \"stderr\")  (a separate drain) can preserve that. receive(c)  on an SSH channel yields the next queued event as a  posix_message  whose  addr  is  \"stdout\" ,  \"stderr\" , or  \"exit\"  and whose  msg  is the payload. The exit status is kept as a  string  so  receive() 's return type stays uniform across every kind of file it can be called on; Unicon's  =  already coerces numeric strings. c := open(s, \"hc\", \"cmd=process_records\") while m := receive(c) do    case m.addr of {       \"stdout\" : write(\"OUT: \" || m.msg)       \"stderr\" : write(\"ERR: \" || m.msg)       \"exit\"   : write(\"done, status \" || m.msg)       } receive()  fails once the channel is exhausted. 8.4 Arrival-order queue Preserving cross-stream order requires libssh's  callback API  ( ssh_channel_callbacks , with a  channel_data_function  receiving an  is_stderr  flag) -- not  ssh_channel_poll()  /  ssh_channel_read() , which buffer stdout and stderr separately and lose interleaving. Callbacks are registered  before  the channel is opened so early data cannot land in libssh's own buffers. Each callback appends a tagged chunk ( SSH_CHUNK_STDOUT  /  STDERR  /  EXIT ) to a heap-allocated linked list on the  SSHfile . Callbacks can fire inside blocking libssh calls made while the thread is unregistered ( DEC_NARTHREADS ), where Unicon allocation is not allowed.  ssh_pump()  drives  ssh_channel_poll_timeout()  so the callbacks run. Stream reads consume stdout chunks;  Attrib(c, \"stderr\")  drains stderr chunks;  receive()  pops whatever is at the head. One mechanism, three accessors. 9. Interactive shells and ``select()`` 9.1 Remote PTY An interactive channel requests a PTY before  shell . Size and  TERM  are described in  . The session file is also marked  Fs_Socket  so existing socket dispatch (including  select()  and  get_fd() ) applies. 9.2 Partial-line ``reads()`` Interactive prompts have no trailing newline. The original socket line helper ( sock_getstrg ) waits for  \n  and hung on a banner or prompt. SSH therefore has its own  ssh_getstrg() : it reads stdout bytes from the queue and returns a partial line at EOF or when the caller's buffer fills, without blocking for a newline that will never come. A seen-but- unconsumed newline is remembered in  nl_pending  (libssh has no  MSG_PEEK ).  reads(s, n)  on a channel is a byte read from the same queue and is the right primitive for an interactive loop. 9.3 ``select()`` readiness select()  on an SSH file must not hang when libssh already buffered the banner during  open() .  ssh_file_pending()  nonblocking-pumps the session and reports ready when stdout (or EOF) is queued. Only stdout matters here: stream  reads()  do not consume stderr/exit chunks, so a nonempty queue of those alone must not make  select()  claim the file is readable.  get_fd()  returns  ssh_get_fd(session)  so the kernel fd participates in the same  select()  set as ordinary sockets. On Windows,  select(&input)  and  Attrib(f, \"tty=raw\")  /  \"tty=sane\"  were added so an interactive client can mux the console with the channel on both Unix and Windows console  iconx . Those tty attributes are general (they apply to  &input ), not SSH-specific. 9.4 ``net::ssh_interactive()`` and ``ussh`` uni/lib/ssh.icn  provides  ssh_interactive(s) : put the local tty in raw mode, mux  &input  with the remote channel via  select() , map lone LF to CRLF when copying remote output to a raw local terminal (needed because raw mode disables  ONLCR ), and restore the tty before returning. It does not close  s . The  ussh  demo ( uni/progs/ussh.icn ) is a small OpenSSH-like client built with the other  uni/progs  demos: interactive shell, or a one-shot remote command via  receive() . s := open(\"user@host\", \"h\", \"key=...\") | stop(&errortext) ssh_interactive(s) close(s) 10. Concurrency and channel lifecycle 10.1 Shared mutex Every  b_file  already has a  mutexid , and all I/O in  fsys.r  already locks around it. When a channel is created via  open(s, \"hc\")  /  open(s, \"hs\") , it does  not  allocate a fresh mutex id -- it copies the session's existing  mutexid . Every channel sharing a session then serializes through the same lock via the existing locking calls in  u_read  /  u_write  /  receive  /  Attrib . libssh sessions are not safe for uncoordinated concurrent access; two threads each holding a different channel on the same session block each other during actual I/O because they contend for the same mutex. 10.2 Child tracking and cascade close A Unicon list of  b_file  pointers was considered so the moving collector would relocate them.  SSHfile  is malloc'd and therefore does not move, so the session owner holds a C linked list of child  SSHfile *  ( children  /  next  /  parent ). No Unicon list is required. close(s)  on the session walks that list, force-closes every remaining channel and SFTP handle (same close-hook logic as an explicit  close() ), marks each child  closed , clears its queue, and then disconnects and frees the session. The child's Unicon  b_file  stays alive until its own  close() , but is unusable immediately -- no dangling libssh objects, no readable leftovers. Closing a channel unlinks it from the owner and frees only that channel. The SFTP subsystem is created lazily, once, on the session owner ( ssh_owner_sftp() ) and shared by every SFTP file or directory on that session. It is freed when the session closes. 10.3 Blocking model I/O uses blocking libssh calls plus Unicon's existing  thread  mechanism -- one thread per in-flight host or channel. Chosen over non-blocking libssh plus  select()  as the  primary  model: it requires no partial-read/retry state machines.  select()  readiness ( ) is implemented so interactive muxing works; it is not a full non-blocking I/O API. 11. SFTP Three shapes, each mapped onto an existing Unicon pattern. 11.1 File transfer -- ``read()`` / ``write()`` c := open(s, \"hs\", \"path=/tmp/firmware.bin\", \"w\") while writes(c, ReadBytes(local_fw)) close(c) Mode  \"hs\"  is SFTP; read/write/append intent comes from the usual mode characters ( r  /  w  /  a  /  b ), either in the same string ( \"hsw\" ) or as a trailing mode-only token.  path=  is required. Backed by  sftp_open()  /  sftp_read()  /  sftp_write()  /  sftp_close() . Structurally a plain file -- another variant feeding the existing dispatch. SFTP regular files are  not  marked  Fs_Socket : they are byte streams, not select-able channels. 11.2 Directory listing Local  open()  on a path that turns out to be a directory transparently switches to  opendir()  /  readdir() , sets  Fs_Directory , and  reads()  yields one entry name per call. SFTP does the same:  open(s, \"hs\", \"path=/tmp/configs\")  checks the remote path with one  sftp_stat() ; if it is a directory,  sftp_opendir()  /  sftp_readdir()  provide the same  Fs_Directory  behavior. 11.3 Metadata -- ``stat()``, ``remove()``, ``rename()`` These extend existing builtins via the leading-optional-file argument idiom, rather than introducing  sftp_remove()  names. The signature change is backward-compatible: a string first argument is still the local-filesystem operation. remove(s, path)  --  sftp_unlink  when  s  is an SSH session or channel file. rename(s, from, to)  --  sftp_rename .  rename  gained a third argument for this form;  rename(s1, s2)  is unchanged. stat(c)  --  sftp_fstat  on an already-open SFTP file. stat(s, path)  -- a single  sftp_stat  request, without paying for  sftp_open()  +  sftp_fstat() . Both entry points are worth supporting rather than just one. if info := stat(s, \"/tmp/firmware.bin\") then    if info.size = expected_size then pull_it() sftp2rec()  populates the same  posix_stat  record as  stat2rec() . SFTP's attribute set is protocol-version dependent; fields the server did not send are left  null  rather than reported as zero, so callers can tell \"absent\" from \"genuinely zero.\" SFTP carries atime/mtime, not ctime. Size, uid/gid (name or numeric), permissions-as-mode-string, and file type ( d  /  l  /  c ) are filled when the corresponding flags are present. 12. Build integration configure --disable-ssh  turns the feature off. Otherwise  CHECK_LIBSSH  in  aclocal.m4  probes for  <libssh/libssh.h>  and  ssh_new , setting  HAVE_LIBSSH . The probe looks under  /usr/local  (FreeBSD) and  /opt/homebrew  (macOS) when those prefixes contain the headers, so later compiles do not miss  libssh/libssh.h .  --with-libssh=DIR  overrides the prefix. A required  --enable-ssh  without the library is a configure error; optional (default) absence degrades gracefully, as with other optional libraries. &features  reports  secure shell  when the library was found.  tests/posix/Makefile  skips  ssh  when the feature is absent. CI installs  libssh-dev  (or the platform equivalent) so the support builds and the deterministic half of the test runs. README install notes list  libssh-dev  /  libssh-devel  /  brew install libssh  alongside the other optional libraries. Guards are  #if HAVE_LIBSSH  throughout  rstructs.h ,  rmacros.h ,  feature.h ,  sys.h ,  fsys.r ,  rposix.r ,  fxposix.ri , and  fmisc.r . 13. Status and remaining work Keyboard-interactive authentication  and an explicit agent attribute. The  publickey_auto  fallback already talks to a running agent. Non-blocking I/O as the primary concurrency model.   select()  readiness is implemented; the I/O calls themselves still block. A  ssh_set_blocking(sess, 0)  plus resume state machine is the obvious next step if thread-count pressure appears. SSH server.  libssh can do it; nothing in the runtime exposes it. The mode character and file-status bit leave room, but the accept/listen path is not designed. Default channel type.  Shell-by-default shipped for mode  \"h\" . Scripted use uses  \"hc\"  with  cmd= . Reconsidering the default remains open and would be a compatibility break. Timeouts.  TCP-connect timeout is the leading integer attribute. There is no separate handshake+auth timeout beyond what that covers, and no exec timeout -- a stuck command blocks until  close() , matching ordinary blocking sockets. Trust-on-first-use.  Verification is check-only ( ssh_session_is_known_server ). A new host fails with 1323 unless the caller uses  h-  or installs the key in  known_hosts  out of band. The runtime does not write the store. ~  in  key= .  Not expanded. The helper and  ussh  expand  ~/  themselves. Appendix: test coverage tests/posix/ssh.icn  is deterministic without a server: it checks that  &features  reports  secure shell , and that bad attributes,  channel=maybe ,  channel=no  combined with  hc / cmd= ,  cmd=  without mode  c , and a connect to a closed local port all fail with  &errortext  set. A live round-trip runs only when  UNICON_SSH_TESTHOST  is set (host or  user@host ), with optional  UNICON_SSH_TESTPORT ,  UNICON_SSH_TESTKEY ,  UNICON_SSH_KNOWNHOSTS , and  UNICON_SSH_SFTPPATH . The live path covers exec stdout,  Attrib(c, \"exitstatus\") ,  receive()  event tags, partial-line  reads()  of a  printf  with no newline, and SFTP write/read/stat/remove of a binary payload. tests/posix/Makefile  skips the test when the feature is absent. Expected default output is  tests/posix/stand/ssh.std . References ","title":"UTR 26: Native SSH and SFTP Support in Unicon"}];
