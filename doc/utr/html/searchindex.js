window.USCRIBE_SEARCH = [{"file":"utr26.html","body":"1. Introduction This report describes the current design and implementation of native SSH in the Unicon runtime: how a session is opened, how additional channels and SFTP handles are derived from it, how stream I/O, ordered events, and metadata operations are exposed, and what remains out of scope. It is formatted as a Unicon Technical Report  . The language-facing reference lives in  Programming with Unicon  (Chapter 6, \"Secure Shell\") and the language reference ( open  mode  h , subscript peek,  key() ,  receive ,  stat  /  remove  /  rename ).   collects examples by mode and use case. Automated tests live in  tests/posix/ssh.icn . The feature is optional. A build with libssh present reports  secure shell  in  &features  (the  _SSH  feature flag). Without the library, mode  h  is ignored and the SSH paths are compiled out, as with other optional features. 2. Motivation and prior art Unicon programs that needed SSH previously relied on  popen()  or pipes to an external  ssh  process. That is not portable (Windows often has no OpenSSH client in  PATH ) and gives the program no structured access to channels, exit status, or SFTP. Native primitives let an application authenticate to hosts, run commands, and transfer files without an external process. 2.1 Library choice Two C libraries were considered.  libssh  (LGPL) supports both client and server, modern key exchange and host keys, and true non-blocking I/O via  poll(2) .  libssh2  (BSD) is client-only and cannot be used fully non-blocking. The implementation binds  libssh   , for server-side optionality later and for the callback API that preserves stdout/stderr arrival order ( ). A full protocol reimplementation (Paramiko, Go's  x/crypto/ssh ) would mean owning crypto primitives and protocol-drift / CVE tracking indefinitely. Binding to libssh via the existing C runtime avoids that. 2.2 Open as the verb Python (Paramiko) and Go ( x/crypto/ssh ) both treat \"new session\" as sugar over \"open another channel on this connection.\" The verb is  open , applied to an existing connection object, not \"clone\" or \"duplicate.\" Unicon already has  open()  as the universal constructor for files, sockets, SSL, and messaging connections.  Clone()  is the Graphics-facility naming convention; a distinct  channel()  function would add a new global name for an operation  open()  already expresses. 2.3 Why not Messaging The Messaging facility (HTTP/POP/SMTP,  rmsg.r , libtp)   is not a fit. Messaging's transport-discipline abstraction has the same 1:1 single-stream limitation as SSL -- no channel-multiplexing concept -- so moving into that world would not solve the multiplexed-channel requirement, only relocate it. SSH's request/channel model ( exec  /  shell  /  sftp  as concurrent channels) also does not map onto Messaging's request/response verb model ( GET  /  POST  /  RETR ). Instead, SSH uses the  h  mode character and plain  user@host  string form, independent of the Messaging/URI subsystem. 3. Architectural grounding The implementation follows established SSL and socket patterns rather than inventing a parallel subsystem. union f  in  src/h/rstructs.h  is the file block's descriptor slot: a tagged union of  FILE * , socket  fd ,  SSL *  when  HAVE_LIBSSL , and so on, discriminated by status bits at runtime. SSH does  not  embed libssh objects directly in the union. It adds a pointer,  struct SSHfile *sshf , matching the Messaging  MFile *  precedent.  SSHfile  is allocated with  malloc  so pointers to it are stable across garbage collection, and is freed by the close hook. A malloc'd block is required once the session must hold a list of child channels whose addresses survive compaction ( ). Status bits live in  src/h/rmacros.h .  Fs_Encrypt  is  0200000000 ; SSH takes the next bit,  Fs_SSH = 0400000000 .  open() 's mode string is scanned character-by-character in  src/runtime/fsys.r .  e  sets  Fs_Encrypt . SSH uses  h  /  H  ( s  is already the Messaging short-request flag). Attributes are  \"key=value\"  strings passed as trailing arguments to  open() , parsed in  create_ssh_session()  the same way  create_ssl_context()  parses SSL attributes -- not a fixed argument list. A leading integer, when present, is a connect timeout in milliseconds, as for plain sockets. All stream I/O funnels through one dispatch point:  u_read()  /  u_write()  in  src/runtime/rposix.r , which already branch on  Fs_Socket  /  Fs_Encrypt . SSH adds a third branch for  Fs_SSH , reading from the arrival-order queue ( ) or  sftp_read() , and writing via  ssh_channel_write()  or  sftp_write() . Nothing above this dispatch ( reads() ,  read() , and so on) needs its own SSH case, except where a line-oriented helper must not wait for a newline that will never arrive ( ). The close hook in  fsys.r  frees SSL resources directly. SSH files follow the same convention -- leaking a channel or session means not calling  close() , the same as any other file. receive()  already returns a general-purpose  posix_message  record ( addr ,  msg ,  saddr ,  daddr ,  ttl ,  proto ) for UDP and raw datagrams. SSH reuses that type for an ordered stdout/stderr/exit-status event stream, using  addr  and  msg  ( ). stat(f)  already  type_case s on its argument (string path versus open file). SFTP extends both branches ( ).  remove()  and  rename()  gain an optional leading session argument, matching the existing Unicon idiom that many functions take an optional leading file or window ( write(\"hello\")  versus  write(f, \"hello\") ). 4. Connection and channel model 4.1 ``open()`` dispatch Two shapes, both using the existing  open() . The second argument is the usual mode string;  h  is a new character in that scan, and  c  /  s  after  h  select a channel or SFTP the same way  u  after  n  selects UDP ( n  is TCP,  nu  is UDP). open(host_or_\"user@host\", \"h...\", attrs...)  -- first argument is a string: opens a new authenticated SSH  session  and, by default, an interactive shell channel on it ( ). open(s, \"hc\"/\"hs\", attrs...)  -- first argument is an existing SSH session or channel file: opens a  new channel  ( hc ) or SFTP handle ( hs ) on the same underlying session (reusing the transport and authentication, no new handshake). Opening on a channel opens a  sibling  on the session owner, not a child of the channel. A session opened with  channel=no  has no channel of its own ( Fs_SSH  alone, no  Fs_Socket ). It exists so later  open(s, \"hc\")  /  open(s, \"hs\")  calls have an authenticated transport.  channel=yes  is the default. Combining  channel=no  with mode  hc  or  cmd=  is contradictory and fails with a bad-attribute error. s := open(\"jafar@\" || host, \"h\", \"key=\" || keypath) write(s, \"ls -l\n\") while line := reads(s) do write(line) close(s) s := open(\"user@host\", \"h\", \"key=id_ed25519\", \"channel=no\") c1 := open(s, \"hc\", \"cmd=uname -a\") c2 := open(s, \"hc\", \"cmd=uptime\") 4.2 Host string The first argument is  host ,  user@host , or either with  :port . The port uses the same  host:port  form as socket  open() ; IPv6 uses  [addr]:port . A bare IPv6 literal is not misparsed: an unbracketed host is split on  :  only when there is a single colon. user@host  wins over a  user=  attribute. If neither is given, libssh's default user (the local login name) is used. 4.3 Default channel: shell, not bare session open(host, \"h\", ...)  with no  cmd=  and without  channel=no  opens a shell on a remote PTY immediately, so the common interactive case is one handle. The PTY is requested with  TERM  (from  term= , else  $TERM , else  xterm ) and a window size (from  cols=  /  rows= , else the local tty via  TIOCGWINSZ , else 80x24). Without a real size, remote  ls(1)  and friends fall back to one column. A shell channel has no clean command boundaries: prompt text, echoed input, and output all interleave. For scripted use, prefer mode  hc  with  cmd= , which has clean stdout and a real exit status: c := open(s, \"hc\", \"cmd=ls -l\") 5. Attributes and authentication Trailing  open()  arguments are  name=value  strings. Empty values and unknown names fail (error 1331). Every attribute must contain  = . Attribute Meaning Notes user= Remote login name Overridden by  user@host key= Path to a private key file Same convention as SSL's  key= keypass= Passphrase for an encrypted private key Same name as TLS/crypto; distinct from SSH  password= password= Remote login password verifyPeer= yes  (default) or  no Same spelling as TLS; overrides mode  - hostkeyfile= Path to an OpenSSH  known_hosts  file Passed to libssh as  SSH_OPTIONS_KNOWNHOSTS cmd= Command for a one-shot exec channel Requires mode  hc ; contradicts  channel=no channel= yes  (default) or  no no  is transport-only term= Remote  TERM  for a PTY Default  $TERM  or  xterm cols=  /  rows= PTY size Default from the local tty, else 80x24 keypass=  is the shared name for a key-file passphrase on TLS, crypto, and SSH. SSH  password=  remains the remote login password. The  -  mode-character flag disables peer verification on TLS ( ne- ) and SSH ( h- ), the same as  verifyPeer=no . An explicit  verifyPeer=  overrides the mode flag.  Fs_Encrypt  and  Fs_SSH  are mutually exclusive contexts, so there is no conflict. A leading integer attribute is a connect timeout in milliseconds and is applied as libssh's  SSH_OPTIONS_TIMEOUT  /  TIMEOUT_USEC . The default port is 22 when  :port  is omitted. 5.1 Authentication order If both  key=  and  password=  are given, they are tried in the order the attributes appear in the call, not in a hardcoded priority. If  neither  is given, the implementation falls back to  ssh_userauth_publickey_auto()  -- default identities under  ~/.ssh  and a running agent -- not to password authentication. Agent use through this fallback is supported; an explicit  agent=  attribute and keyboard-interactive auth remain out of scope. key=  does not expand  ~ . Callers (and the  ussh  demo) must expand a leading  ~/  themselves; libssh treats the path literally. 6. Examples and use cases These examples assume a Unicon build with the  secure shell  feature. Connection failures fail with  &errortext  set; the usual form is  s := open(...) | stop(&errortext) . The packaged demo  uni/progs/ussh.icn  is a small client that covers the interactive and one-shot command cases. 6.1 Modes Like every other  open() , SSH uses the second argument as a mode string (it defaults to  \"r\"  when omitted, which is not useful for SSH).  h  /  H  is a mode character in that same scan, alongside  r  /  w  /  n  /  e  /  m . After  h ,  c  selects a command/channel and  s  selects SFTP -- the same \"facility letter, then modifier\" order as  nu  (UDP) and  ms  (Messaging short-request).  n  alone is TCP. Without a preceding  h ,  c  is still create+write. Trailing  \"name=value\"  arguments are attributes, the same as SSL uses for  key=  and friends. Mode characters combine.  \"h-\"  is SSH plus skip host-key verification (the  -  flag also disables SSL peer checks).  \"hc\"  is a channel (exec when  cmd=  is given, else a shell).  \"hs\"  is SFTP;  r  /  w  /  a  /  b  set transfer intent ( \"hsw\" ). Form Kind Handle open(host, \"h\", ...) mode  h New session plus a PTY shell open(host, \"h-\", ...) mode  h  and  - Same, no host-key check open(host, \"hc\", \"cmd=...\") mode  hc , attribute  cmd= New session plus an exec channel open(host, \"h\", \"channel=no\") mode  h , attribute  channel= Session only (no channel) open(s, \"hc\") mode  hc  on an existing SSH file Extra shell channel on that session open(s, \"hc\", \"cmd=...\") mode  hc , attribute  cmd= Extra exec channel on that session open(s, \"hsw\", \"path=...\") mode  hs  plus access chars SFTP file or directory open(host, \"hsw\", \"path=...\") mode  hs  on a host string New session plus SFTP (one handle) leading integer attribute (timeout) Connect timeout in milliseconds channel=no  with  hc  or  cmd=  fails (error 1331).  cmd=  without  c  also fails. 6.2 Interactive shell With no  cmd= ,  open(..., \"h\")  requests a remote PTY and shell.  net::ssh_interactive(s)  attaches the local tty (raw mode,  select()  with  &input , CRLF for display): import net procedure main()    s := open(\"user@host\", \"h\", \"key=/home/user/.ssh/id_ed25519\") |         stop(&errortext)    ssh_interactive(s)    close(s) end term= ,  cols= , and  rows=  override the PTY. Mode  \"h-\"  skips  known_hosts  (useful on a first connect before the key is installed). Password auth uses  password=  instead of, or in addition to,  key= . 6.3 Remote command Mode  \"hc\"  with  cmd=  runs one command and returns its stdout through ordinary  read()  /  reads() .  c[\"exitstatus\"]  is the remote exit code once it has arrived: procedure main()    s := open(\"user@host\", \"hc\", \"key=/home/user/.ssh/id_ed25519\",              \"cmd=uname -a\") | stop(&errortext)    while write(read(s))    write(\"exit status: \", s[\"exitstatus\"])    close(s) end The host string may include a port ( user@host:2222 ) or an IPv6 address ( user@[2001:db8::1]:22 ). A leading integer is a connect timeout in milliseconds: s := open(\"user@host\", \"hc\", 5000, \"cmd=true\") | stop(&errortext) 6.4 Ordered stdout and stderr Stream  reads()  see stdout only. When stdout and stderr must stay in arrival order, or when stderr should go to  &errout , use  receive() : s := open(\"user@host\", \"hc\", \"key=/home/user/.ssh/id_ed25519\",           \"cmd=echo out; echo err 1>&2; exit 3\") | stop(&errortext) while m := receive(s) do    case m.addr of {       \"stdout\" : writes(&output, m.msg)       \"stderr\" : writes(&errout, m.msg)       \"exit\"   : write(&errout, \"[exit \", trim(m.msg), \"]\")       } close(s) c[\"stderr\"]  is the other stderr path: it peeks the current accumulated buffer, without preserving order against stdout. See  . 6.5 Several channels on one session channel=no  opens an authenticated transport. Later  open(s, \"hc\")  /  open(s, \"hs\")  calls add exec, shell, or SFTP handles on that session. Closing  s  invalidates every derived handle: s := open(\"user@host\", \"h\", \"key=/home/user/.ssh/id_ed25519\",           \"channel=no\") | stop(&errortext) c1 := open(s, \"hc\", \"cmd=uname -a\") | stop(&errortext) c2 := open(s, \"hc\", \"cmd=uptime\") | stop(&errortext) while write(read(c1)) while write(read(c2)) close(c1) close(c2) close(s) Opening on an existing channel opens a sibling on the session owner, not a child of that channel. 6.6 SFTP The same session carries SFTP. Mode  \"hs\"  plus  path=  opens a remote file or, if the path is a directory, lists it.  stat ,  remove , and  rename  take the session as a leading argument: s := open(\"user@host\", \"h\", \"key=/home/user/.ssh/id_ed25519\",           \"channel=no\") | stop(&errortext) c := open(s, \"hs\", \"path=/tmp/out.bin\", \"w\") | stop(&errortext) writes(c, ReadBytes(\"local.bin\")) close(c) info := stat(s, \"/tmp/out.bin\") | stop(&errortext) write(\"remote size: \", info.size) d := open(s, \"hs\", \"path=/tmp\") | stop(&errortext) while write(read(d)) close(d) rename(s, \"/tmp/out.bin\", \"/tmp/out.bin.bak\") remove(s, \"/tmp/out.bin.bak\") close(s) 6.7 Concurrent hosts Each connection is blocking. Concurrent hosts use Unicon  thread , one thread per in-flight session: procedure run_cmd(host, key)    local s, line    s := open(host, \"hc\", \"key=\" || key, \"cmd=uname -a\") | {       write(&errout, host, \": \", &errortext)       fail       }    while line := read(s) do write(host, \": \", line)    close(s) end procedure main()    local host, key    key := \"/home/user/.ssh/id_ed25519\"    every host := \"user@alpha\" | \"user@beta\" | \"user@gamma\" do       thread run_cmd(host, key) end Channels that share a session serialize on the session mutex ( ). Separate sessions do not. 7. Error handling Connection and channel failures  fail , they do not  runerr . This matches the general  open()  pattern: set  &errortext , tear down what was allocated, and fail, so callers write  if s := open(...) then ... else ... . Runtime errors are reserved for type mistakes (passing a non-SSH file to  open(s, \"hc\") , a bad peek name on an SSH file). Error numbers, defined in  src/runtime/data.r : Number Text Typical cause 1330 SSH error Connect failure, allocation, bad host string 1331 bad ssh attribute Unknown name, empty value,  cmd=  without  c ,  channel=no  with  hc 1332 SSH authentication error No method succeeded 1333 SSH host key verification error Server not in  known_hosts  (when verifying) 1334 SSH channel error Channel open, I/O, or a closed/cascaded handle 1335 SFTP error SFTP subsystem, path, or transfer failure set_ssh_errortext()  appends libssh's own message where available. Blocking libssh calls run under  DEC_NARTHREADS ;  &errortext  is set only after the thread is re-registered, because Unicon allocation is not allowed while unregistered. 8. I/O: streams, peek fields, and ``receive()`` Three tiers, layered simple-default to full-fidelity. All three share one per-channel event queue populated by libssh callbacks ( ). 8.1 Plain stream I/O read()  /  write()  /  reads()  /  writes()  work unmodified via the existing  u_read()  /  u_write()  dispatch, extended with an  Fs_SSH  branch. Stream reads consume  stdout only . They are enough when the caller does not need to distinguish stderr or preserve cross-stream order. Writes to a channel call  ssh_channel_write()  and retry short writes. Writes to a transport-only session (no channel) fail with SFTP regular files use  sftp_write()  on the same path. 8.2 Peek fields (``[ ]`` and ``key()``) Status on SSH files is a non-destructive peek through  [ ] . Unknown names raise 1331. An unpopulated field fails. Boolean fields that answered succeed with  \"yes\"  or  &null .  key(c)  generates every answerable field, including false booleans.  c[\"*\"]  snapshots those fields under one lock. Peeking a closed handle is error 174;  close()  keeps the original name so  image(c)  remains  file(user@host) .  key(c)  that has not yet produced a name also raises 174 if the handle is already closed. After the first suspend, Similar to messaging:  !  generates content,  key()  generates names. Channels keep line-generating  !  (they carry  Fs_Socket ). Transport-only sessions ( channel=no ) are not line streams;  !s  fails. A few fields are lists: one item is still a list of length 1. Walk members with  !s[\"authmethods\"] . Channel  (exec, shell, or SFTP handle).  c[\"type\"]  is  channel  or  sftp . Name Value type channel  or  sftp exitstatus Integer remote exit code. **Fails until the exit-status stderr Accumulated stderr (a peek, not a drain). Fails if empty. eof \"yes\"  once the remote sent EOF, else  &null . bytesread  /  byteswritten Running counters (integers). Session  (the authenticated connection, including  channel=no ). Name Value type session fingerprint Server host-key SHA-256, e.g.  SHA256:x3Gnt... authmethods List of method names ( publickey ,  password , cipher Negotiated inbound cipher: kex Key-exchange algorithm:  curve25519-sha256 , mac Inbound MAC:  hmac-sha2-256 ,  hmac-sha2-512 serverbanner Server identification string, e.g.  SSH-2.0-OpenSSH_9.6 connected \"yes\"  or  &null s := open(\"user@host\", \"h\", \"key=id_rsa\", \"channel=no\") |    stop(&errortext) write(\"type=\", s[\"type\"], \" kex=\", s[\"kex\"]) every m := !s[\"authmethods\"] do    write(\"auth \", m) every k := key(s) do    write(\"session field \", k) c := open(s, \"hc\", \"cmd=apply-config /tmp/newconfig.json\") while line := reads(c) do    process(line) if c[\"exitstatus\"] = 0 then    write(\"ok\") else    write(\"failed: \" || (c[\"stderr\"] | \"\")) every k := key(c) do    write(\"channel field \", k) close(c) close(s) Preferring callback state over libssh's blocking  get_exit_*  helpers avoids version-sensitive fallbacks once callbacks are registered. Failing until the status is ready reuses the ordinary Unicon \"fail = not ready\" idiom, the same as  read()  failing at EOF. 8.3 ``receive()`` for ordered events A command can produce interleaved stdout and stderr where the caller needs to know  when  an stderr message occurred relative to stdout. Neither  reads()  (stdout only) nor  c[\"stderr\"]  (a separate peek) can preserve that. receive(c)  on an SSH channel yields the next queued event as a  posix_message  whose  addr  is  \"stdout\" ,  \"stderr\" , or  \"exit\"  and whose  msg  is the payload. The exit status is kept as a  string  so  receive() 's return type stays uniform across every kind of file it can be called on; Unicon's  =  already coerces numeric strings. c := open(s, \"hc\", \"cmd=process_records\") while m := receive(c) do    case m.addr of {       \"stdout\" : write(\"OUT: \" || m.msg)       \"stderr\" : write(\"ERR: \" || m.msg)       \"exit\"   : write(\"done, status \" || m.msg)       } receive()  fails once the channel is exhausted. 8.4 Arrival-order queue Preserving cross-stream order requires libssh's  callback API  ( ssh_channel_callbacks , with a  channel_data_function  receiving an  is_stderr  flag) -- not  ssh_channel_poll()  /  ssh_channel_read() , which buffer stdout and stderr separately and lose interleaving. Callbacks are registered  before  the channel is opened so early data cannot land in libssh's own buffers. Each callback appends a tagged chunk ( SSH_CHUNK_STDOUT  /  STDERR  /  EXIT ) to a heap-allocated linked list on the  SSHfile . Callbacks can fire inside blocking libssh calls made while the thread is unregistered ( DEC_NARTHREADS ), where Unicon allocation is not allowed.  ssh_pump()  drives  ssh_channel_poll_timeout()  so the callbacks run. Stream reads consume stdout chunks;  c[\"stderr\"]  peeks stderr chunks;  receive()  pops whatever is at the head. One mechanism, three accessors. 9. Interactive shells and ``select()`` 9.1 Remote PTY An interactive channel requests a PTY before  shell . Size and  TERM  are described in  . The session file is also marked  Fs_Socket  so existing socket dispatch (including  select()  and  get_fd() ) applies. 9.2 Partial-line ``reads()`` Interactive prompts have no trailing newline. The original socket line helper ( sock_getstrg ) waits for  \n  and hung on a banner or prompt. SSH therefore has its own  ssh_getstrg() : it reads stdout bytes from the queue and returns a partial line at EOF or when the caller's buffer fills, without blocking for a newline that will never come. A seen-but- unconsumed newline is remembered in  nl_pending  (libssh has no  MSG_PEEK ).  reads(s, n)  on a channel is a byte read from the same queue and is the right primitive for an interactive loop. 9.3 ``select()`` readiness select()  on an SSH file must not hang when libssh already buffered the banner during  open() .  ssh_file_pending()  nonblocking-pumps the session and reports ready when stdout (or EOF) is queued. Only stdout matters here: stream  reads()  do not consume stderr/exit chunks, so a nonempty queue of those alone must not make  select()  claim the file is readable.  get_fd()  returns  ssh_get_fd(session)  so the kernel fd participates in the same  select()  set as ordinary sockets. On Windows,  select(&input)  and  Attrib(f, \"tty=raw\")  /  \"tty=sane\"  were added so an interactive client can mux the console with the channel on both Unix and Windows console  iconx . Those tty attributes are general (they apply to  &input ), not SSH-specific. 9.4 ``net::ssh_interactive()`` and ``ussh`` uni/lib/ssh.icn  provides  ssh_interactive(s) : put the local tty in raw mode, mux  &input  with the remote channel via  select() , map lone LF to CRLF when copying remote output to a raw local terminal (needed because raw mode disables  ONLCR ), and restore the tty before returning. It does not close  s . The  ussh  demo ( uni/progs/ussh.icn ) is a small OpenSSH-like client built with the other  uni/progs  demos: interactive shell, or a one-shot remote command via  receive() . s := open(\"user@host\", \"h\", \"key=...\") | stop(&errortext) ssh_interactive(s) close(s) 10. Concurrency and channel lifecycle 10.1 Shared mutex Every  b_file  already has a  mutexid , and all I/O in  fsys.r  already locks around it. When a channel is created via  open(s, \"hc\")  /  open(s, \"hs\") , it does  not  allocate a fresh mutex id -- it copies the session's existing  mutexid . Every channel sharing a session then serializes through the same lock via the existing locking calls in  u_read  /  u_write  /  receive  /  Attrib . libssh sessions are not safe for uncoordinated concurrent access; two threads each holding a different channel on the same session block each other during actual I/O because they contend for the same mutex. 10.2 Child tracking and cascade close A Unicon list of  b_file  pointers was considered so the moving collector would relocate them.  SSHfile  is malloc'd and therefore does not move, so the session owner holds a C linked list of child  SSHfile *  ( children  /  next  /  parent ). No Unicon list is required. close(s)  on the session walks that list, force-closes every remaining channel and SFTP handle (same close-hook logic as an explicit  close() ), marks each child  closed , clears its queue, and then disconnects and frees the session. The child's Unicon  b_file  stays alive until its own  close() , but is unusable immediately -- no dangling libssh objects, no readable leftovers. Closing a channel unlinks it from the owner and frees only that channel. The SFTP subsystem is created lazily, once, on the session owner ( ssh_owner_sftp() ) and shared by every SFTP file or directory on that session. It is freed when the session closes. 10.3 Blocking model I/O uses blocking libssh calls plus Unicon's existing  thread  mechanism -- one thread per in-flight host or channel. Chosen over non-blocking libssh plus  select()  as the  primary  model: it requires no partial-read/retry state machines.  select()  readiness ( ) is implemented so interactive muxing works; it is not a full non-blocking I/O API. 11. SFTP Three shapes, each mapped onto an existing Unicon pattern. 11.1 File transfer -- ``read()`` / ``write()`` c := open(s, \"hs\", \"path=/tmp/firmware.bin\", \"w\") while writes(c, ReadBytes(local_fw)) close(c) Mode  \"hs\"  is SFTP; read/write/append intent comes from the usual mode characters ( r  /  w  /  a  /  b ), either in the same string ( \"hsw\" ) or as a trailing mode-only token.  path=  is required. Backed by  sftp_open()  /  sftp_read()  /  sftp_write()  /  sftp_close() . Structurally a plain file -- another variant feeding the existing dispatch. SFTP regular files are  not  marked  Fs_Socket : they are byte streams, not select-able channels. 11.2 Directory listing Local  open()  on a path that turns out to be a directory transparently switches to  opendir()  /  readdir() , sets  Fs_Directory , and  reads()  yields one entry name per call. SFTP does the same:  open(s, \"hs\", \"path=/tmp/configs\")  checks the remote path with one  sftp_stat() ; if it is a directory,  sftp_opendir()  /  sftp_readdir()  provide the same  Fs_Directory  behavior. 11.3 Metadata -- ``stat()``, ``remove()``, ``rename()`` These extend existing builtins via the leading-optional-file argument idiom, rather than introducing  sftp_remove()  names. The signature change is backward-compatible: a string first argument is still the local-filesystem operation. remove(s, path)  --  sftp_unlink  when  s  is an SSH session or channel file. rename(s, from, to)  --  sftp_rename .  rename  gained a third argument for this form;  rename(s1, s2)  is unchanged. stat(c)  --  sftp_fstat  on an already-open SFTP file. stat(s, path)  -- a single  sftp_stat  request, without paying for  sftp_open()  +  sftp_fstat() . Both entry points are worth supporting rather than just one. if info := stat(s, \"/tmp/firmware.bin\") then    if info.size = expected_size then pull_it() sftp2rec()  populates the same  posix_stat  record as  stat2rec() . SFTP's attribute set is protocol-version dependent; fields the server did not send are left  null  rather than reported as zero, so callers can tell \"absent\" from \"genuinely zero.\" SFTP carries atime/mtime, not ctime. Size, uid/gid (name or numeric), permissions-as-mode-string, and file type ( d  /  l  /  c ) are filled when the corresponding flags are present. 12. Build integration configure --disable-ssh  turns the feature off. Otherwise  CHECK_LIBSSH  in  aclocal.m4  probes for  <libssh/libssh.h>  and  ssh_new , setting  HAVE_LIBSSH . The probe looks under  /usr/local  (FreeBSD) and  /opt/homebrew  (macOS) when those prefixes contain the headers, so later compiles do not miss  libssh/libssh.h .  --with-libssh=DIR  overrides the prefix. A required  --enable-ssh  without the library is a configure error; optional (default) absence degrades gracefully, as with other optional libraries. &features  reports  secure shell  when the library was found.  tests/posix/Makefile  skips  ssh  when the feature is absent. CI installs  libssh-dev  (or the platform equivalent) so the support builds and the deterministic half of the test runs. README install notes list  libssh-dev  /  libssh-devel  /  brew install libssh  alongside the other optional libraries. Guards are  #if HAVE_LIBSSH  throughout  rstructs.h ,  rmacros.h ,  feature.h ,  sys.h ,  fsys.r ,  rposix.r ,  fxposix.ri , and  fmisc.r . 13. Status and remaining work Keyboard-interactive authentication  and an explicit agent attribute. The  publickey_auto  fallback already talks to a running agent. Non-blocking I/O as the primary concurrency model.   select()  readiness is implemented; the I/O calls themselves still block. A  ssh_set_blocking(sess, 0)  plus resume state machine is the obvious next step if thread-count pressure appears. SSH server.  libssh can do it; nothing in the runtime exposes it. The mode character and file-status bit leave room, but the accept/listen path is not designed. Default channel type.  Shell-by-default shipped for mode  \"h\" . Scripted use uses  \"hc\"  with  cmd= . Reconsidering the default remains open and would be a compatibility break. Timeouts.  TCP-connect timeout is the leading integer attribute. There is no separate handshake+auth timeout beyond what that covers, and no exec timeout -- a stuck command blocks until  close() , matching ordinary blocking sockets. Trust-on-first-use.  Verification is check-only ( ssh_session_is_known_server ). A new host fails with 1333 unless the caller uses  h-  or installs the key in  known_hosts  out of band. The runtime does not write the store. ~  in  key= .  Not expanded. The helper and  ussh  expand  ~/  themselves. Appendix: test coverage tests/posix/ssh.icn  is deterministic without a server: it checks that  &features  reports  secure shell , and that bad attributes,  channel=maybe ,  channel=no  combined with  hc / cmd= ,  cmd=  without mode  c , and a connect to a closed local port all fail with  &errortext  set. A live round-trip runs only when  UNICON_SSH_TESTHOST  is set (host or  user@host ), with optional  UNICON_SSH_TESTPORT ,  UNICON_SSH_TESTKEY ,  UNICON_SSH_KNOWNHOSTS , and  UNICON_SSH_SFTPPATH . The live path covers exec stdout,  c[\"exitstatus\"] ,  receive()  event tags, partial-line  reads()  of a  printf  with no newline, and SFTP write/read/stat/remove of a binary payload. tests/posix/Makefile  skips the test when the feature is absent. Expected default output is  tests/posix/stand/ssh.std . References ","title":"UTR 26: Native SSH and SFTP Support in Unicon"},{"file":"utr27.html","body":"1. Introduction This report describes the cryptographic facilities in the Unicon runtime: how a handle is opened, how payload flows through  write()  /  read() , how key and certificate material is loaded and reused, and how the same letters and attributes compose with encrypted sockets. It is formatted as a Unicon Technical Report  . The language-facing reference lives in  Programming with Unicon  (Chapter 6) and the language reference ( open  modes  e  /  er  /  eh  /  re  /  we , and  ne  /  nue , subscript peek,  key() ). Automated tests live under  tests/crypto/ . The feature is optional in the same sense as TLS. A build with OpenSSL reports  secure sockets layer encryption  in  &features  (the  _SSL  flag). Without the library, the crypto paths are compiled out. 2. Motivation Unicon already links OpenSSL   for mode  n  with  e  -- encrypted TCP. Programs that needed a SHA-256 digest, an HMAC, a signature, or a file encrypted at rest had to use external tools. The facilities here expose those primitives without a second API: hashing a stream is opening a handle and writing to it; reading an encrypted file is opening the file with a transform on the way through. No new external dependency is introduced. The implementation uses OpenSSL's EVP interface, which the encrypted-socket support already requires. 3. Design principles e  is the crypto letter, reused not invented.  Unicon already uses  e  for encryption ( Fs_Encrypt ). New modes carry that letter rather than adding one letter per operation.  op=  selects what the handle does. e  composes as a modifier.  Mode letters already stack, and several already branch on bits accumulated so far:  n  +  e  is an encrypted socket;  r  +  e  /  w  +  e  is a file transform;  e  +  h  is an in-memory hash;  e  +  r  is raw key material rather than a path. The open target configures the operation; payload is I/O.  Target is never the message being hashed or encrypted. What target means depends on the mode (algorithm name, key path, data file, host:port). Payload always flows through  write()  /  read()  /  reads() . Attributes are  name=value  strings, or handles.  A string element is parsed as always. A  handle  -- the return value of a prior  open(..., \"e\")  -- is a typed attribute that could not survive being flattened into a string. Handles are interpreted by  role  from their own content (private key, public key, cert, symmetric key), not by position or by an attribute name. Material comes from a file, raw bytes, or a handle.  A file path is the target under mode  e , or  key=  when the target is already spoken for ( re  /  we ,  n  +  e ). Raw bytes use mode  er . A handle avoids re-parsing. Initialization is lazy.   open()  does not create the underlying EVP context until the first  write()  (or the first transforming  read()  on a file). A handle that is never used costs nothing.  read()  finalizes and resets, so the next  write()  initializes again. Attrib()  reconfigures only in the idle window  -- before the first  write() , or after a  read()  and before the next  write() . Changing  op= ,  alg= ,  cipher= , or  iv=  mid-operation fails. Handles are accepted by  open() . 4. Modes open() 's mode string is scanned character by character in  src/runtime/fsys.r . Letters are not independent:  u  after  n  is UDP,  r  after  n  is raw,  h  after  e  is hash. Order matters.  e  already set  Fs_Encrypt ; this design adds  Fs_Crypto  for hash / HMAC / sign / verify / encrypt-decrypt handles so they are distinct from TLS sockets that also carry  Fs_Encrypt . Mode Meaning Target e Load material, or an operation whose target is material Key/cert path, or a material handle er Same, but the target is raw bytes In-memory string eh Hash; no  op=  needed Digest name, e.g.  sha256 re Read a file, transformed (hash or decrypt) Data-file path we Write a file, transformed (encrypt) Data-file path ne Encrypted TCP (TLS) -- unchanged host:port nue  /  naue Encrypted UDP (DTLS) host:port e  /  er  are the  data-pipe  shape: write payload in, read the result out.  eh  is separate because its target is an algorithm name, not material.  re  /  we  are the  file-transform  shape: the target is the data file, so key material arrives via  key=  or a handle attribute. h  alone remains SSH.  r  alone remains read. The new meanings apply only when  e  was seen first. Mode  -  skips peer verification on TLS ( ne- ) and SSH ( h- ), the same as  verifyPeer=no . An explicit  verifyPeer=  overrides the mode flag. 5. Operations op=  names the operation when the mode does not already imply one. Loading material ( e  /  er  with a file or raw target and no  op= ), hashing in memory ( eh ), and TLS / DTLS sockets take no  op= . op= What it does Typical mode Other attributes hash Digest of a file re alg= hmac Keyed digest (RFC 2104) e  /  er alg= sign Produce a signature e  /  er alg= verify Check a signature e  /  er alg= ,  sig= encrypt Symmetric encrypt e  /  er , or  we cipher= ;  key=  or a handle for  we decrypt Symmetric decrypt e  /  er , or  re cipher= ;  key=  or a handle for  re HMAC is the construction of RFC 2104   over the digest named by  alg= . Algorithm and cipher names are OpenSSL's own ( sha256 ,  aes-256-gcm ) and are passed through rather than mapped to a Unicon-defined set. Defaults are  alg=sha256  and  cipher=aes-256-gcm . 6. Attributes Trailing  open()  arguments are  name=value  strings or crypto handles. Empty values and unknown names fail (TLS attribute errors use 1302; crypto state/name mistakes use 1316). 6.1 TLS / DTLS attributes These are the names  create_ssl_context()  accepts. They apply to  ne  /  nue  and are unchanged except for the two alignments in  . Attribute Meaning cert= Certificate file path key= Private key file path keypass= Passphrase for an encrypted private key ca=  /  caDir=  /  caStore= Trust store ciphers=  /  ciphers1.3= TLS 1.2 list (e.g.  HIGH ,  ECDHE-RSA-AES256-GCM-SHA384 ) minProto=  /  maxProto= Protocol version bounds, e.g.  TLS1.2 ,  TLS1.3 verifyPeer= yes  /  no ;  no  is the same as mode  - Socket attributes ( reuseaddr ,  ttl ,  iface ,  join ,  proto , ...) are applied separately and are unaffected. Live-session status is  . 6.2 Crypto attributes Attribute Meaning Used with op= Operation:  hash ,  hmac ,  sign ,  verify , All data-pipe and file-transform ops alg= Digest name passed to OpenSSL (default  sha256 ; hash ,  hmac ,  sign ,  verify cipher= Symmetric cipher (default  aes-256-gcm ; also encrypt ,  decrypt iv= Explicit IV; omit for automatic IV encrypt ,  decrypt sig= Signature being checked verify key= Key path when the target is already a data file or host re  /  we , and TLS type= Narrow a multi-item file:  cert ,  key ,  pubkey Material load ( e  /  er ) keypass= Passphrase for an encrypted key file Material load key=  on a crypto handle is typed by content and by the operation: PEM / DER private keys for  sign  and TLS; raw bytes for  encrypt ,  decrypt , and  hmac . A role mismatch fails. Without  iv= ,  encrypt  generates a fresh random IV per operation and prepends it to the ciphertext;  decrypt  reads that prefix. Supplying  iv=  opts out -- useful for a wire format that does not prepend an IV, or for test vectors -- and hands uniqueness back to the caller. The default cipher is AEAD; the runtime appends the authentication tag after the ciphertext. Status peeks are  . 6.3 Shared names with SSH key=  is a private-key path on TLS, crypto, and SSH.  keypass=  is the shared name for a key-file passphrase. SSH verifyPeer=yes|no  and mode  -  mean the same thing on TLS and SSH. An explicit  verifyPeer=  overrides the mode flag. Trust stores stay distinct: TLS uses  ca=  (X.509); SSH uses  hostkeyfile=  (OpenSSH  known_hosts ). 6.4 Status peek (``[ ]`` and ``key()``) h[\"name\"]  /  conn[\"name\"]  is a non-destructive get.  Attrib()  only assigns in the idle window ( op= ,  alg= ,  sig= , ...). Unknown names raise 1302 on a crypto handle and 1326 on a TLS socket. An unpopulated field fails. A boolean field that  did  answer succeeds:  \"yes\"  for true,  &null  for false , which would make things like  if h[\"expired\"]  succeed on a valid certificate.  key(h)  generates every  answerable  field, including false booleans, so any  k  from  key(h)  makes  h[k]  succeed. Dump with  image(h[k])  so  &null  is visible. h[\"*\"]  returns a table of those same fields captured under one lock. No status field is named  * . Peeking a closed handle is error 174.  key(h)  that has not yet produced a name also raises 174 if the handle is already closed. After the first suspend,  close()  makes the generator fail instead of raising, so a walk does not turn a mid-generation close into an error. h[\"type\"]  is a role label ( key ,  cert ,  key,cert ), not a list.  h[\"san\"]  and  conn[\"san\"]  are lists of SAN strings ( DNS:host ,  IP:1.2.3.4 ).  conn[\"certchain\"]  is a list of PEM strings. A single item is still a list of length 1.  h[\"op\"]  is always populated on an operation handle. Material  (mode  e  /  er , no  op= ). Name Value type key ,  pubkey ,  cert ,  symkey , or a alg Public-key algorithm:  rsaEncryption , keysize Size in bits (integer), e.g.  2048 ,  256 subject  /  issuer Certificate DN, OpenSSL oneline san List of Subject Alternative Names ( DNS:localhost , notbefore  /  notafter Validity timestamps ( 2024-01-01T00:00:00Z ) expired \"yes\"  or  &null fingerprint SHA-256 of the cert or public key (hex) Certificate names fail on a key-only handle. Hash  ( eh , or  op=hash ). Name Value op hash alg Digest name:  SHA256  (default),  SHA512 ,  SHA1 , hash Current digest / MAC / signature bytes without bytecount Bytes written so far (integer) blocksize  /  digestsize Algorithm sizes (integers); SHA-256 is 64 / 32 HMAC, sign, verify. Name Value op hmac ,  sign , or  verify hash Running HMAC or signature bytes (same copy-and-finalize alg Digest used for HMAC or for the signature:  SHA256 , bytecount Bytes written (integer) verified \"yes\"  or  &null  after a verify; unpopulated before Encrypt / decrypt. Name Value op encrypt  or  decrypt cipher Symmetric cipher:  AES-256-GCM  (default), iv IV that was set or generated (bytes) bytecount Bytes processed (integer) TLS socket  (after handshake). These are not  open()  attributes. Name Value cipher Negotiated suite:  TLS_AES_256_GCM_SHA384 , version TLSv1.3 ,  TLSv1.2 alpn Selected protocol:  h2 ,  http/1.1 ; unpopulated if none peercert Peer certificate as PEM certchain List of PEMs subject  /  issuer Peer DN, e.g.  /CN=host.example san Peer SANs as a list ( DNS:host.example ) notbefore  /  notafter  /  expired Peer validity;  expired  is  \"yes\"  or  &null certverified \"yes\"  or  &null . Always populated: with verifyresult ok ,  expired ,  self-signed ,  untrusted-CA , hostnamematch \"yes\"  or  &null : did CN/SAN match the requested Handshake failure means  open()  failed; there is no handle to subscript. Distinguish expired / untrusted CA / hostname mismatch / no shared cipher / protocol version via  &errornumber  1320--1325. cert := open(\"client.crt\", \"e\") | stop(&errortext) write(\"material \", cert[\"type\"], \" \", cert[\"subject\"]) every name := !cert[\"san\"] do    write(\"  SAN \", name) every k := key(cert) do    write(\"  \", k) h := open(\"sha256\", \"eh\") | stop(&errortext) write(h, \"abc\") write(\"op=\", h[\"op\"], \" peek hash *\", *h[\"hash\"]) close(h) conn := open(\"host:443\", \"ne\", \"ca=unicon-ca.crt\") | stop(&errortext) write(conn[\"cipher\"], \" \", conn[\"version\"]) every pem := !conn[\"certchain\"] do    write(*pem, \" byte PEM\") close(conn) 7. Material handles Mode  e  with no  op=  loads keys and certificates. Content is auto-detected (PEM by header, DER by probe-by-parse). A file that holds several items -- a combined cert+key PEM, or a cert plus chain -- loads into one handle.  type=  narrows that to a single role. k  := open(\"signing.pem\", \"e\") pk := open(\"verify.pem\", \"e\") c  := open(\"client.pem\", \"e\") kb := open(key_bytes, \"er\") ek := open(\"enc.key\", \"e\", \"keypass=\" || pw) c  := open(\"bundle.pem\", \"e\", \"type=cert\") A raw key is still a file value;  h[\"type\"]  reports the role: procedure main()    local k    k := open(\"sixteen-byte-key-material-here!!\", \"er\") |       stop(&errortext)    write(type(k))    write(k[\"type\"])    close(k) end Output: file symkey Material split across files can be merged: idh := open(\"client.pem\", \"e\") Attrib(idh, \"key=client.key\") A handle is a file value ( type(h)  is  \"file\" ) carrying  Fs_Crypto , the same way a window or socket is a file that does not support every file operation. The underlying  CryptoFile  is malloc'd so pointers stay stable across garbage collection and are freed by  close() . Consumers ask a handle for the roles they need. An encrypted socket wants a cert and a private key: one merged handle satisfies both, or two handles each contribute what they have.  op=sign  wants a private key;  op=hmac  wants symmetric bytes. A missing role, or two handles supplying the same role, fails at  open() . 8. Examples These assume a build with  secure sockets layer encryption . Failures set  &errortext ; the usual form is  h := open(...) | stop(&errortext) . Package  crypto  ( uni/lib/crypto.icn ) supplies  hexencode  /  hexbytes  and base64 helpers for dumping binary digests and keys. The programs under  tests/crypto/  are the executable form of this section. 8.1 Hash SHA-256 of the known vector  abc : import crypto procedure main()    local h, digest    h := open(\"sha256\", \"eh\") | stop(&errortext)    write(h, \"abc\")    digest := read(h)    write(hexbytes(digest, 1))    close(h) end Output: ba7816bf8f01cfea414140de5dae2223b00361a396177a9cb410ff61f20015ad write()  may be called many times;  read()  finalizes the digest  and resets  the handle, so the same handle can hash unrelated messages. Chunked  \"hel\"  +  \"lo\"  matches a single write of  \"hello\" . A read with nothing pending fails: import crypto procedure main()    local h, d1, d2    h := open(\"sha256\", \"eh\") | stop(&errortext)    write(h, \"hello\")    d1 := hexbytes(read(h), 1)    write(h, \"hel\")    write(h, \"lo\")    d2 := hexbytes(read(h), 1)    write(d1)    write(d2)    if read(h) then write(\"idle:fail\") else write(\"idle:ok\")    close(h) end Output: 2cf24dba5fb0a30e26e83b2ac5b9e29e1b161e5c1fa7425e73043362938b9824 2cf24dba5fb0a30e26e83b2ac5b9e29e1b161e5c1fa7425e73043362938b9824 idle:ok A whole file uses the file-transform shape. The first  read()  consumes the file and returns the digest: import crypto procedure main()    local f, h, digest    f := open(\"hash.dat\", \"w\") | stop(&errortext)    writes(f, \"abc\")    close(f)    h := open(\"hash.dat\", \"re\", \"op=hash\", \"alg=sha256\") |       stop(&errortext)    digest := read(h)    write(hexbytes(digest, 1))    close(h)    remove(\"hash.dat\") end Output: ba7816bf8f01cfea414140de5dae2223b00361a396177a9cb410ff61f20015ad 8.2 HMAC Raw key bytes use mode  er .  read()  returns the MAC; the same handle can MAC many messages after each finalize: import crypto procedure main()    local hm, mac    hm := open(\"secret-key-bytes\", \"er\", \"op=hmac\", \"alg=sha256\") |       stop(&errortext)    write(hm, \"message one\")    mac := read(hm)    write(hexbytes(mac, 1))    write(*mac)    close(hm) end Output: 9fd1a900325bd173af31bf6467de8139d6615202654cf0fe2645cd2997eaf879 32 A key loaded from a file is the same, with the material handle as the  open()  target and  op=hmac  as an attribute: k  := open(\"hmac.key\", \"e\") hm := open(k, \"e\", \"op=hmac\", \"alg=sha256\") 8.3 Sign and verify A private key signs; a certificate or public key verifies.  read()  on the signer returns the signature bytes. The listing assumes  signing.pem  and  verify.pem  in the current directory: procedure main()    local k, sg, pk, vf, signature    k := open(\"signing.pem\", \"e\") | stop(&errortext)    sg := open(k, \"e\", \"op=sign\", \"alg=sha256\") | stop(&errortext)    write(sg, \"hello\")    signature := read(sg) | stop(&errortext)    close(sg)    pk := open(\"verify.pem\", \"e\") | stop(&errortext)    vf := open(pk, \"e\", \"op=verify\", \"alg=sha256\",       \"sig=\" || signature) | stop(&errortext)    write(vf, \"hello\")    if read(vf) then write(\"ok\") else stop(\"bad signature: \",       &errortext)    close(vf)    close(k)    close(pk) end Output: ok A failed verify sets  &errornumber  1315 so it is distinct from \"nothing written.\" The same handle can check many messages by setting  sig=  in the idle window: procedure main()    local k, sg, pk, vf, signature, msg    k := open(\"signing.pem\", \"e\") | stop(&errortext)    sg := open(k, \"e\", \"op=sign\", \"alg=sha256\") | stop(&errortext)    pk := open(\"verify.pem\", \"e\") | stop(&errortext)    vf := open(pk, \"e\", \"op=verify\", \"alg=sha256\") |       stop(&errortext)    every msg := ![\"one\", \"two\"] do {       write(sg, msg)       signature := read(sg) | stop(&errortext)       Attrib(vf, \"sig=\" || signature)       write(vf, msg)       if read(vf) then write(msg, \":ok\") else write(msg, \":fail\")       }    every close(sg | vf | k | pk) end Output: one:ok two:ok 8.4 Symmetric encrypt and decrypt The idle-window  Attrib(..., \"op=decrypt\")  reuses the bound key. Ciphertext is binary; the recovered plaintext is what matters: procedure main()    local ky, c, ct, pt    ky := open(\"sixteen-byte-key-material-here!!\", \"er\") |       stop(&errortext)    c := open(ky, \"e\", \"op=encrypt\") | stop(&errortext)    write(c, \"hello\")    ct := read(c)    Attrib(c, \"op=decrypt\")    write(c, ct)    pt := read(c)    write(pt)    close(c)    close(ky) end Output: hello With no  iv= , each  read()  generates a fresh IV and prepends it, so one handle can encrypt many messages. Lengths below include the IV and AEAD tag: procedure main()    local ky, c, msg, ct    ky := open(\"sixteen-byte-key-material-here!!\", \"er\") |       stop(&errortext)    c := open(ky, \"e\", \"op=encrypt\") | stop(&errortext)    every msg := ![\"alpha\", \"beta\", \"gamma\"] do {       write(c, msg)       ct := read(c)       write(msg, \" \", *ct, \" bytes\")       }    close(c)    close(ky) end Output: alpha 33 bytes beta 32 bytes gamma 33 bytes For a wire format that does not prepend an IV, or for test vectors, set  iv=  in the idle window and take uniqueness yourself: every plaintext := !messages do {    Attrib(c, \"iv=\" || newiv())    write(c, plaintext)    ciphertext := read(c)    } 8.5 File encrypt and decrypt Writes encrypt and are finalized on  close() . Reads decrypt as a stream. AEAD authentication is checked on the  final  read: earlier  reads()  chunks are provisional. Nothing decrypted should be acted on until the stream completes without error. procedure main()    local ky, f, chunk, pending    ky := open(\"sixteen-byte-key-material-here!!\", \"er\") |       stop(&errortext)    f := open(\"secrets.dat\", \"we\", \"op=encrypt\", ky) |       stop(&errortext)    writes(f, \"classified\")    close(f)    f := open(\"secrets.dat\", \"re\", \"op=decrypt\", ky) |       stop(&errortext)    pending := \"\"    while chunk := reads(f, 4096) do       pending ||:= chunk    if  &errornumber then       stop(\"tampered: \", &errortext)    write(pending)    close(f)    remove(\"secrets.dat\")    close(ky) end Output: classified Callers who cannot defer processing should use the data-pipe shape, where a single  read()  returns plaintext only after the tag verifies. The same key handle can wrap many files: keyh := open(\"aes.key\", \"e\") every name := !filenames do {    f := open(name || \".enc\", \"we\", \"op=encrypt\", keyh)    write(f, contents(name))    close(f)    } 8.6 TLS identity reuse and DTLS Encrypted TCP remains  \"ne\" . Material handles may be passed as attributes so certs and keys are parsed once. The socket asks by role, not by position: one combined PEM, or two handles, both work. id := open(\"client.pem\", \"e\") conn := open(\"host:443\", \"ne\", id, \"verifyPeer=no\") conn := open(\"host:443\", \"ne-\", id)    # same as verifyPeer=no certh := open(\"client.pem\", \"e\") keyh  := open(\"client.key\", \"e\") conn  := open(\"host:443\", \"ne\", certh, keyh) After a handshake,  conn[\"cipher\"]  peeks the negotiated suite and  conn[\"certchain\"]  is a list of peer certificates as PEM strings; see   ( tests/crypto/tlspeek.icn ).  certh[\"type\"]  /  keyh[\"type\"]  are material-handle peeks, not session fields. Encrypted UDP (DTLS) uses  \"nue\"  /  \"naue\" . The runtime selects a DTLS context when the socket is datagram. 9. I/O and ``Attrib()`` On a data-pipe handle,  write()  accumulates and  read()  finalizes.  read()  is always terminal -- one read consumes the whole result and resets the handle -- for two reasons that are easy to forget later. First, the default cipher is AEAD ( aes-256-gcm ): nothing authenticated can be released until the tag is checked at the end, so a mid-stream plaintext read would be a lie. Second,  read()  already means \"finalize and reset\" for hash / HMAC / sign; a partial encrypt read would look identical to a final one, and the handle would reset under the caller. Block ciphers such as CBC or CTR  could  emit ciphertext at a block boundary, but that would be a second meaning of  read()  next to the AEAD and digest rules, so it is not offered. A second  read()  with nothing written since the last one fails, as when reading a socket with no data.  close()  is idempotent: a second  close() , or  close()  on a never-written handle, releases the handle and drops its reference to any bound key. On  we , writes encrypt to the file and  close()  flushes the AEAD tag. On  re  with  op=decrypt ,  reads()  yields plaintext chunks. On  re  with  op=hash , the first  read()  returns the digest. Attrib()  during the idle window may change  op= ,  alg= ,  cipher= ,  iv= , and  sig= . A change while a digest or cipher is in flight fails (1316). Status gets are  . Handles are not valid  Attrib()  values. Framing is owned by the runtime: IV prepended, tag appended, both fixed-length per cipher. The same layout is used for  e  /  er  output and for  we  files. 10. Error handling Crypto and TLS connect failures  fail , they do not  runerr . This matches the SSL connect path: set  &errortext , tear down what was allocated, and fail, so callers write  if h := open(...) then ... else ... . Runtime errors are reserved for type mistakes (a bad mode letter, a non-file where a handle is required). Error numbers, defined in  src/runtime/data.r : Number Text Typical cause 1300--1308 SSL / certificate / cipher errors TLS context and handshake 1311 unknown cryptographic algorithm or cipher Bad  alg=  or  cipher= 1312 cryptographic handle lacks required material Missing key, cert, or wrong role 1313 duplicate cryptographic material role Two handles both supply the same role 1314 AEAD authentication failed Tampered or truncated ciphertext 1315 signature verification failed op=verify  did not match 1316 invalid cryptographic operation state Mid-operation  Attrib() , bad  op= , empty read 1317 cryptographic material could not be loaded Unreadable path, parse failure, allocation 11. Build integration Crypto compiles under  HAVE_LIBSSL , the same guard as TLS.  configure --disable-ssl  turns both off.  --enable-thin  disables SSL as part of a minimal build. Otherwise  CHECK_OPENSSL  probes for the library and headers. &features  reports  secure sockets layer encryption .  tests/crypto/Makefile  skips the suite when that string is absent. TLS echo tests also require  concurrent threads . Guards are  #if HAVE_LIBSSL  in  rstructs.h ,  rmacros.h ,  feature.h ,  fsys.r ,  fmisc.r ,  rposix.r , and  rcrypto.ri . TLS helpers ( create_ssl_context ,  is_ssl_attr ) and the  CryptoFile  implementation share  src/runtime/rcrypto.ri . 12. Status and remaining work The language surface described here is implemented: modes  e  /  er  /  eh  /  re  /  we , TLS handle reuse, DTLS context selection and handshake,  keypass=  /  verifyPeer=  alignment with SSH, and the error numbers in  . Separate configure switch.  Crypto cannot be compiled out while leaving TLS sockets on. A  Unicon_Crypto  /  NoCrypto  guard driven by  --disable-crypto  is the obvious next step if a distribution wants encrypted sockets without the new  open()  modes. ~  in  key= .  Not expanded, matching SSH. Callers must expand a leading  ~/  themselves. Asymmetric encrypt / decrypt.  The  op=  set is hash, HMAC, sign, verify, and symmetric encrypt/decrypt. Public-key encryption is not exposed. Trust-on-first-use and key generation  are out of scope. Certificates and keys are created out of band. Appendix: test coverage Automated tests live under  tests/crypto/ , one program per concern:  hash ,  hmac ,  sha512 ,  sign ,  encrypt ,  iv ,  freshiv ,  tamper ,  filehash ,  filecrypt ,  filekey ,  filekeyattr ,  filetamper ,  opswitch ,  midattrib ,  badalg ,  badkey ,  badrole ,  badverify , plus TLS / DTLS ( tlsplain ,  tlsverify ,  tlsauth ,  tlsproto ,  tlscipher ,  tlshandle ,  tlspeek ,  dtls ,  dtlsecho ).  tests/crypto/Makefile  skips the suite when OpenSSL is absent, and skips the threaded TLS echo tests when concurrency is absent. Expected output is  tests/crypto/stand/*.std . References ","title":"UTR 27: Cryptographic Facilities in Unicon"},{"file":"utr28.html","body":"1. Introduction This report describes IP multicast   and the socket-attribute facility it sits on. Trailing arguments to  open()  on network modes are  name=value  strings applied with  setsockopt(2) . After  open() ,  Attrib()  uses the same names. The language-facing reference lives in  Programming with Unicon    (Chapter 5, \"Multicast and Socket Attributes\") and the language reference ( open  trailing attributes,  Attrib , subscript peek,  key() ). Automated tests live in  tests/posix/mcast.icn  and  tests/posix/sockpeek.icn . 2. Motivation Unicon's  open()  already constructed TCP and UDP handles. Multicast -- any-source (ASM) and source-specific (SSM)   -- is ordinary UDP plus membership and hop-limit options. Without those options, a Unicon program could not join a multicast group like  239.1.1.1 , restrict a join to one NIC, or send with a limited TTL like  ttl=3 . These options were not supported. 3. Design principles Attributes are  name=value , like graphics windows.  Unknown names fail  open()  with error 1310 ( bad socket attribute ). Booleans are exactly  yes  or  no , matching  verifyPeer . procedure main()    if open(\":5110\", \"nua4\", \"bogus=yes\") then       write(\"oops\")    else       write(\"bogus: \", &errortext)    if open(\":5110\", \"nua4\", \"reuseaddr=yes\") then       write(\"oops\")    else       write(\"numeric boolean: \", &errortext) end Output: bogus: bad socket attribute numeric boolean: bad socket attribute DWIM for the common cases.  Binding a UDP socket to a multicast group joins that group. Listeners default to  reuseaddr=yes . No  iface=  means join every UP IPv4 interface and send out the first non-loopback IPv4 address (else loopback). Sending to  255.255.255.255  enables  SO_BROADCAST . Explicit attributes always override. source@group .  The address form matches VLC, ffmpeg, and RFC 4607  (S,G)  order. Empty either side is invalid. ttl  and  iface , not multicast-only names.  Hop limit and interface are used by unicast, multicast, and raw sockets  .  ttl=  sets both  IP_TTL  and  IP_MULTICAST_TTL  (or the IPv6 hop options).  iface=  accepts an IPv4 address, a numeric index, or a name ( en0 ,  lo0 ; Windows also maps  lo  /  lo0  to  loopback_0 ). Order matters.   iface=  before  join=  selects the interface used by that join.  Attrib()  applies a batch of assignments together for the same reason. Cached listeners are aliases; attributes get a new socket.  A second  open()  of the same listener address with no socket attributes returns another file for the cached fd. An open that supplies  join=  /  iface=  / and so on creates an independent socket so it cannot retune earlier aliases. Use  Attrib()  to change options on a live shared handle. 4. Opening a multicast socket IP multicast uses UDP (modes  nu  /  nua , optionally with  4  or  6  for family). A minimal receiver and sender: procedure main()    local f, r    f := open(\"239.1.1.1:5000\", \"nua\") | stop(&errortext)    while r := receive(f) do       write(r.addr, \"  \", r.msg) end procedure main()    local f, i    f := open(\"239.1.1.1:5000\", \"nu\") | stop(&errortext)    every i := 1 to 5 do       writes(f, \"hello-\" || i) & delay(500) end The receiver binds the group address, so the runtime joins that group (ASM) and enables port reuse. The sender's destination is the group; without  iface= , outbound multicast uses the first non-loopback IPv4 address. Kernel multicast loopback stays on, so a local receiver still sees the packets. The same exchange in one process over loopback (this is the shape of  tests/posix/mcast.icn ).  iface=127.0.0.1  keeps the datagram on loopback even when the host has no multicast route: procedure main()    local f, s, r    f := open(\"239.42.42.99:5199\", \"nua\", \"iface=127.0.0.1\") |         stop(&errortext)    s := open(\"239.42.42.99:5199\", \"nu\", \"iface=127.0.0.1\") |         stop(&errortext)    writes(s, \"hello-multicast\")    if *select(f, 2000) > 0 then {       r := receive(f)       write(r.msg)       }    else       write(\"timeout\")    close(s)    close(f) end Output: hello-multicast r.addr  is the sender (host plus an ephemeral port). A wildcard bind plus explicit joins is the other shape: f := open(\":5000\", \"nua\",           \"iface=192.168.1.10\",           \"join=239.1.1.1\",           \"join=232.1.1.1,10.0.0.5\") join=group  is ASM;  join=group,source  is SSM. Attributes may be repeated. Mixing ASM and SSM in one call is allowed. If  open()  has no  4  /  6  flag, a  join=  group address picks the family so an IPv4 join is not attempted on an IPv6 wildcard socket. 5. Source-specific multicast Name the source before the group, or pass  source=  on a socket already bound to the group. Both IPv4 and IPv6 are supported. For IPv6 SSM, pass an explicit  iface=  (name or index) so the join uses a single interface: f := open(\"192.168.1.1@239.1.1.1:5000\", \"nua\") f := open(\"239.1.1.1:5000\", \"nua\", \"source=192.168.1.1\") f := open(\"::1@ff3e::1:5000\", \"nua6\", \"iface=lo0\") f := open(\"ff3e::1:5000\", \"nua6\", \"iface=lo0\", \"source=::1\") On send or connect,  source@group:port  keeps only the group so the destination is the group address. The source is a receive-side filter, not a send address. IPv6 SSM is a single  MCAST_JOIN_SOURCE_GROUP  /  MCAST_LEAVE_SOURCE_GROUP  with no multi-interface walk. Those walks have triggered macOS kernel panics in IPv6 source filter teardown. Already-a-member and already-gone are treated as success. 6. Attributes Attribute Role join= Add membership:  group  (ASM) or  group,source leave= Drop membership; same forms as  join . Pass source= SSM source for a group bind (repeatable). Set-only. iface= IPv4 address, index, or name. Restricts join and ttl= Unicast and multicast hop limits.  f[\"ttl\"]  peeks mcastloop=yes|no Whether the host receives its own multicasts. Kernel reuseaddr=  /  reuseport= Bind reuse. Listeners default to  reuseaddr=yes broadcast= SO_BROADCAST . Opening  255.255.255.255  sets it. rcvbuf=  /  sndbuf= Buffer sizes in bytes. 7. ``Attrib()`` and status peek procedure main()    local f, g    f := open(\":5110\", \"nua4\") | stop(&errortext)    Attrib(f, \"ttl=4\")    write(\"ttl=\", f[\"ttl\"])    write(\"mcastloop=\", f[\"mcastloop\"])    if g := f[\"groups\"] then       every write(!g)    else       write(\"groups: unpopulated\")    close(f) end Output: ttl=4 mcastloop=yes groups: unpopulated Assignments in one call are applied together, preserving  iface  then  join  order. Status is peeked with  f[\"name\"] . Unknown names raise An unpopulated field fails. Boolean fields that answered succeed with  \"yes\"  or  &null .  key(f)  generates every answerable field.  f[\"*\"]  snapshots those fields under one lock. Peeking a closed handle is error 174.  key(f)  that has not yet produced a name also raises 174 if the handle is already closed. TCP, UDP, multicast, and raw sockets share one peek table ( sock_peek ).  join ,  leave , and  source  are verbs, not peek fields.  proto  is stored at  socket()  time on raw sockets  .  f[\"groups\"]  is a list of joined groups (SSM as  source@group ); a single group is still a list of length 1. Name Value ttl Hop limit (integer), typically  1  ..  255 iface Dotted IPv4 address ( 127.0.0.1 ) or IPv6 ifindex ( 1 ) groups List of joined groups, e.g.  239.1.1.1 ; SSM as mcastloop \"yes\"  or  &null reuseaddr  /  reuseport \"yes\"  or  &null  ( reuseport  may be unpopulated) broadcast \"yes\"  or  &null rcvbuf  /  sndbuf Buffer sizes in bytes (integers) proto IP protocol number:  1  (ICMP),  2  (IGMP),  89 hdrincl \"yes\"  or  &null ; typical on raw sockets f := open(\":5110\", \"nua4\") | stop(&errortext) Attrib(f, \"ttl=4\") Attrib(f, \"iface=127.0.0.1\", \"join=239.1.1.1\") Attrib(f, \"iface=127.0.0.1\", \"join=239.1.1.2\") write(\"ttl=\", f[\"ttl\"]) every g := !f[\"groups\"] do    write(\"joined \", g) every k := key(f) do    write(\"field \", k) close(f) Membership changes still use assignment form: Attrib(f, \"join=239.1.1.2\") Attrib(f, \"iface=127.0.0.1\", \"leave=239.1.1.1\") On some Linux/musl builds  getsockopt(IP_MULTICAST_IF)  does not recover the interface used at join time. Pass  iface=  on  leave=  so  IP_DROP_MEMBERSHIP  matches the join. Already-not-a-member is success; the runtime also retries  INADDR_ANY  and each local IPv4 address when a single DROP fails. 8. Listener cache sock_listen  still caches the first bind of an address so repeated  open()  of the same listener is cheap. That cache is a problem if a later  open(..., \"join=...\")  reused the fd and changed memberships for every live alias. Rules: Cache hit, no socket attributes: another  File  for the same fd (owner refcount). Closing one alias leaves the others until the last handle is closed. Cache hit with attributes: release the pin and create an independent socket ( reuseaddr  makes the second bind succeed).  leave=  on that socket does not drop the cached alias's membership. Changing options on a shared handle:  Attrib()  on one of the aliases. A cache-hit open that is not a pure alias only allows additive  join=  /  source= ;  leave=  and option changes on that path would retune every live  File . 9. Runtime implementation All of this lives in  src/runtime/rposix.r , applied from  open()  in  fsys.r  and from  Attrib()  in  fmisc.r . Two passes.   reuseaddr  /  reuseport  must be set between  socket()  and  bind() . Everything else runs after the socket is bound.  apply_sock_attrs()  takes a  prebind  flag. Auto-join.  After the post-bind pass, a UDP socket bound to a multicast group is joined unless an explicit  join=  took over.  source@group  or  source=  makes that join SSM. IPv4 join without  iface= .  Walk  getifaddrs()  and  IP_ADD_MEMBERSHIP  on every UP IPv4 address so same-host and multi-homed receives work without naming a NIC. An explicit  iface=  joins only that one. Outbound multicast without  iface=  picks the first non-loopback IPv4 address via  IP_MULTICAST_IF . IPv6 ASM  uses  IPV6_JOIN_GROUP  /  IPV6_LEAVE_GROUP  with an ifindex (zero means default). There is no all-iface walk like IPv4. Errors.  Unknown attributes and bad booleans are 1310.  setsockopt  failures fail  open()  /  Attrib()  with the system  &errortext , except idempotent membership errors (already a member / already gone). Mode  n  with  4  or  6  still selects the address family.  \"nua4\"  is useful when a join group must match an IPv4 wildcard. 10. Status and remaining work The language surface described here is implemented: trailing attributes, implicit ASM/SSM joins,  Attrib()  join/leave, listener-cache isolation, IPv4 all-iface join, IPv6 SSM with an explicit interface, and the broadcast shortcut. Membership query.   f[\"groups\"]  is a list of groups joined through  open()  or  Attrib()   join=  /  source= . The kernel still has no portable membership dump; this is runtime-tracked state. IPv6 all-interface join.  IPv4 walks every NIC; IPv6 does not. Callers who need every IPv6 interface must join per  iface= . IPv6 SSM without  iface= .  Not recommended. The implementation refuses to walk interfaces for IPv6 source filters. Loopback delivery.  Alpine (especially under QEMU) often does not deliver loopback multicast even when the join succeeds.  tests/posix/mcast.icn  still checks membership and treats a missing datagram as success on Alpine so CI stays portable. Windows IPv6 SSM.  Membership calls are attempted;  loopback_0  and UNIX  lo  /  lo0  names are mapped. Delivery and option support vary by stack; the test skips cleanly on  ENOPROTOOPT . Raw sockets    reuse  ttl  /  iface  /  join  on  SOCK_RAW . That is a separate report. Appendix: test coverage tests/posix/mcast.icn  runs in one process over loopback, one UDP port per case so a late datagram cannot land on the next receiver. It checks: unknown attributes and numeric booleans fail with 1310 explicit  join=  plus  iface=127.0.0.1 implicit join by binding the group address SSM via  source@group  and via  source= broadcast open of  255.255.255.255  (never  EACCES ) Attrib()   ttl= ,  join= ,  leave= IPv6 SSM membership via  @ ,  source= , and  join=group,source  (delivery not required) cache-hit alias vs independent attr open tests/posix/sockpeek.icn  checks  f[\"ttl\"] ,  f[\"groups\"]  as a list after two joins, and  key(f) . Expected output is  tests/posix/stand/mcast.std : bogus attribute: bad socket attribute numeric boolean: bad socket attribute Received hello-multicast Received hello-implicit Received hello-ssm-at Received hello-ssm-source broadcast: ok ttl=3 Received hello-attrib leave: ok ipv6-ssm-at: ok ipv6-ssm-source: ok ipv6-ssm-join: ok cache-attr open: independent Received hello-alias References ","title":"UTR 28: Multicast and Socket Attributes in Unicon"},{"file":"utr29.html","body":"1. Introduction This report describes raw IP sockets in Unicon: how a handle is opened, how datagrams are sent and received, how protocol bytes are built and decoded, and what remains out of scope. It is formatted as a Unicon Technical Report  . Socket attributes ( ttl ,  iface ,  join , and the rest) are the multicast facility  ; this report adds mode  nr  and the packet library on top of that. The language-facing reference lives in  Programming with Unicon    (Chapter 5, \"Raw Sockets\" and \"PacketSpec and package net\"; Chapter 14,  uping  and  uigmp ) and the language reference ( open  mode  nr , attributes  proto  and  hdrincl , subscript peek). Automated plumbing and peek tests live in  tests/posix/raw_sock.icn . Raw sockets are part of the POSIX network facility. A build without POSIX networking has no  n  modes at all. Opening  SOCK_RAW  usually needs elevated privileges (root or administrator); that is an operating-system restriction, not a Unicon one. 2. Motivation TCP and UDP cover most application protocols. Diagnostic and control traffic sits one layer down: ICMP echo  , IGMP membership    , OSPF, GRE, PIM. Those messages are IP protocol payloads, not port-addressed transport streams. ICMP, IGMP, and other raw IP protocols were not supported. The rest of Unicon networking already treats  open()  as the constructor and  Attrib()  as the option verb. Raw sockets follow that pattern instead of adding  rawsocket()  or a parallel packet API. Payload construction is a library problem: the runtime delivers bytes, and  PacketSpec  names the fields. 3. Design principles r  after  n  is the socket type, like  u  for UDP.  Mode letter  r  remains ordinary read mode unless a preceding  n  has already marked the handle as a network socket. Then  \"nr\"  means  SOCK_RAW , the same way  \"nu\"  means  SOCK_DGRAM .  \"rn\"  is not a raw socket:  r  is consumed as read mode before  n  is seen. proto=  is required and is fixed at  socket()  time.  The third argument to  socket(2)  is the IP protocol number. There is no useful default: ICMP, IGMP, and OSPF are different sockets. A missing, empty, or unknown  proto=  is error 1310 ( bad socket attribute ).  f[\"proto\"]  peeks the protocol number stored at  open() ;  Attrib()  cannot change it. The runtime does not build protocol payloads.  Writes send whatever bytes the caller supplies. Receives return whatever the kernel delivered, often including an IPv4 header on Linux.  import net  and  PacketSpec  (or hand-assembled strings) fill that gap. Datagram I/O matches UDP.  Raw sockets are not connected.  writes()  uses  sendto(2)  with the destination saved at  open() . Incoming packets use  receive() , which returns a record with  addr ,  msg ,  saddr ,  daddr , and (for IPv4 raw datagrams that include a header)  ttl  and  proto . Those extra fields are also available as  r.ttl  /  r.saddr . Use  receive() , not  read()  /  reads() . Two layers for callers.  Session wrappers ( Ping ,  Igmp ) hide mode letters for the common cases. PacketSpec classes are there when the program must craft or classify bytes itself. 4. Opening a raw socket f := open(\"8.8.8.8\", \"nr\", \"proto=icmp\", \"ttl=64\") |      stop(&errortext) The first argument is a bare host or address, not  host:port . Raw destinations are hosts. A single  host:port  form is accepted when there is exactly one colon and a non-empty port, but ICMP and IGMP do not use ports. IPv6 literals contain colons; they are treated as hosts and are not split as  host:port . proto=  takes a symbolic name or an integer 0--255: Name Protocol icmp IPPROTO_ICMP icmpv6  /  icmp6 IPPROTO_ICMPV6  (fails if the host has no constant) igmp IPPROTO_IGMP tcp  /  udp IPPROTO_TCP  /  IPPROTO_UDP gre 47 ospf 89 pim IPPROTO_PIM raw IPPROTO_RAW 0  ..  255 that protocol number Names are case-insensitive. Unknown names and numbers outside 0--255 fail at  open()  with 1310. If the kernel refuses  SOCK_RAW ,  open()  fails and  &errortext  is the system message (typically \"Operation not permitted\" or \"Permission denied\"). That is success of the Unicon plumbing and a privilege problem on the host. procedure main()    local f    if f := open(\"8.8.8.8\", \"nr\") then       write(\"oops missing proto\")    else       write(\"missing-proto: \", &errortext)    if f := open(\"8.8.8.8\", \"nr\", \"proto=nope\") then       write(\"oops bad proto\")    else       write(\"bad-proto: \", &errortext)    if f := open(\"8.8.8.8\", \"nr\", \"proto=icmp\") then       write(\"raw-icmp: ok\")    else       write(\"raw-icmp: \", &errortext) end Output: missing-proto: bad socket attribute bad-proto: bad socket attribute raw-icmp: Operation not permitted The last line is the unprivileged case. With privileges it prints  raw-icmp: ok  (or the test suite's  plumbing-ok ). 5. Attributes and status peek Trailing  open()  arguments are the same  name=value  socket attributes used for TCP and UDP  . Booleans are exactly  yes  or  no .  ttl  and  iface  were named for this sharing: hop limit and interface are not multicast-only knobs. Attribute Role on a raw socket proto= Required. Consumed at  socket() . Peek with hdrincl=yes Sets  IP_HDRINCL  so writes may include a complete IPv4 ttl= Unicast and multicast hop limits ( IP_TTL  / rcvbuf=  /  sndbuf= Socket buffer sizes in bytes. broadcast=  /  mcastloop= Same meaning as UDP. join=  /  leave=  /  source=  /  iface= Multicast membership, including SSM  source@group Attrib(f, \"ttl=4\")  sets hop limits after  open() . Status uses the same  [ ]  /  key()  get as UDP  : unknown names raise 1310, unpopulated fields fail. Boolean peek fields that answered succeed with  \"yes\"  or  &null  (never  \"no\" ); that is output only --  open()  and  Attrib()  still take  yes  or  no . Peeking a closed handle is error 174.  key(f)  that has not yet produced a name also raises 174 if the handle is already closed; after the first suspend,  close()  makes the generator fail instead of raising. Raw sockets add  proto  (fixed at  socket() ) and  hdrincl . Name Peek value proto IP protocol number from  proto= :  1  ( icmp ), hdrincl \"yes\"  or  &null  (not  \"no\" ) ttl Hop limit (integer), e.g.  64 iface Outbound / join interface, e.g.  127.0.0.1 groups List of joined groups, e.g.  239.1.1.1 ; unpopulated mcastloop  /  reuseaddr  /  reuseport  / \"yes\"  or  &null  (not  \"no\" ) rcvbuf  /  sndbuf Buffer sizes in bytes (integers) f := open(\"127.0.0.1\", \"nr\", \"proto=icmp\") | stop(&errortext) write(\"proto=\", f[\"proto\"]) write(\"ttl=\", f[\"ttl\"] | \"unpopulated\") every k := key(f) do    write(\"field \", k) close(f) 6. Send and receive writes(f, icmp_bytes) r := receive(f) write(r.addr, \"  \", *r.msg, \" bytes\") writes()  calls  sock_write() , which for  SOCK_RAW  (and UDP) does  sendto  to the  addrinfo  saved at  open() . There is no  connect(2) . Changing the destination means opening another handle (or, with  hdrincl=yes , putting the destination in a supplied IP header). receive()  peeks with  MSG_PEEK  into a 2K buffer, then consumes with  recvfrom . If the peek fills 2K, the consume uses a 64K buffer so a large datagram is not truncated. The result is a  posix_message :  addr  is the peer (often  a.b.c.d:0  for raw IPv4),  msg  is the byte string.  saddr  /  daddr  use the usual IP-header names: on an IPv4 raw receive they are the header source and destination; on UDP,  saddr  is the numeric peer address and  daddr  is unpopulated. When the receive buffer starts with an IPv4 header,  ttl  and  proto  are that header's hop limit and protocol; otherwise those fields are null. On many IPv4 stacks a raw receive buffer starts with the IP header, then the protocol message. Linux ICMP is the usual example. Strip it before decoding ICMP or IGMP ( ). IPv6 raw sockets typically deliver the upper layer only; there is no IPv6 header-strip helper yet. select()  works on the handle. Session wrappers use it to implement timeouts around  receive() . As with UDP,  read()  /  reads()  are the wrong verbs for datagrams. Use  receive() . 7. PacketSpec Module  packetspec  (package  net ,  uni/lib/packetspec.icn ) is a small declarative engine for fixed binary layouts. Protocol classes inherit  PacketSpec  and override  define() . Layer-1 helpers pack and unpack network-order integers ( u8  /  u16  /  u32  /  u64  and the matching  get_u*  readers) and IPv4 addresses ( ipv4  /  to_ipv4  /  get_ipv4 ).  inet_cksum()  is RFC 1071  . 7.1 Declaring a layout class IcmpEchoRequest : IcmpPacket()    method define()       field(\"type\", 1, 8)       field(\"code\", 1, 0)       checksum_field(\"checksum\")       field(\"id\", 2)       field(\"seq\", 2)    end end field(name, size, defval)  is a network-order integer.  bytes_field  is opaque fixed-width bytes.  ipv4_field  accepts a dotted-quad, integer, or four raw bytes on build and returns a dotted-quad on decode.  checksum_field  is a 16-bit placeholder filled at  build()  time over the header plus payload. Pass  exclude_off  /  exclude_len  (1-based) to omit a range from the sum --- OSPF Authentication and PIM Register are the documented cases; those layouts are not in the library yet. require(name)  marks a field that  build()  must see via  set()  or the optional extra table.  relax()  /  build(..., lax)  skip that check for incomplete test packets.  strict()  and  clear()  turn checking back on. 7.2 Build, decode, describe Preferred call shape (no string-keyed tables required). This needs no raw socket: import net procedure main()    local pkt, v    write(IcmpEchoRequest().describe())    write(IcmpEchoRequest().set(\"id\", 1).set(\"seq\", 2).describe())    pkt := IcmpEchoRequest().set(\"id\", 1).set(\"seq\", 2).build(, \"hi\")    write(\"len=\", *pkt)    v := IcmpEchoRequest().decode(pkt)    write(\"type=\", v[\"type\"], \" id=\", v[\"id\"], \" seq=\", v[\"seq\"]) end Output: type:int=8  code:int=0  checksum:checksum(auto)  id:int=0  seq:int=0 type:int=8  code:int=0  checksum:checksum(auto)  id:int=1  seq:int=2 len=10 type=8 id=1 seq=2 set()  and  set_payload()  are chainable.  build(extra, payload, lax)  overlays an optional table, appends payload, and returns the complete byte string with checksum filled.  decode(s)  returns a table of field names to values.  describe()  is a one-line summary for the REPL: names, kinds, defaults, required markers, and current  set()  values. 7.3 IPv4 receive helpers strip_ipv4(pkt)  uses the header IHL to return the upper-layer message.  ipv4_ttl(pkt)  and  ipv4_src(pkt)  read TTL and source from the same leading header. ICMP and IGMP classes call  strip_ipv4  before matching. 8. ICMP echo uni/lib/icmp.icn  adds echo request (type 8) and echo reply (type 0)   on top of  PacketSpec : PacketSpec   IcmpPacket          strip_ip / match     IcmpEchoRequest   type 8; echo(id, seq, payload)     IcmpEchoReply     type 0; match_echo(pkt, id, seq) Ping                  session wrapper Ping(host)  opens  proto=icmp  with TTL 64 and a 2000 ms default wait.  echo()  sends a request (56 zero bytes unless a payload is given), waits with  select() , and succeeds with a decode table plus  rtt  (milliseconds, real),  rtt_us ,  ttl  (from the IPv4 header), and  addr . Round-trip time uses  gettimeofday() . import net procedure main()    local p, v    p := Ping(\"8.8.8.8\") | stop(&errortext)    write(p.describe())    if v := p.echo() then       write(\"rtt=\", v[\"rtt\"], \" from \", v[\"addr\"])    p.close() end set_timeout(ms)  changes the default wait. An explicit sequence number and payload can be passed to  echo() ; otherwise the session increments  seq . The same exchange with an explicit socket: import net procedure main()    local f, req, reply, id, pkt, r    req := IcmpEchoRequest()    reply := IcmpEchoReply()    id := iand(?100000 | 1, 16rFFFF)    f := open(\"8.8.8.8\", \"nr\", \"proto=icmp\", \"ttl=64\") |         stop(&errortext)    pkt := req.set(\"id\", id).set(\"seq\", 1).build(, \"unicon\")    writes(f, pkt)    r := receive(f)    if reply.match_echo(r.msg, id, 1) then       write(\"reply from \", r.addr)    close(f) end Other ICMP types (destination unreachable, time exceeded, and so on) are not in the library.  proto=icmpv6  opens an ICMPv6 socket, but there are no Neighbor Discovery or echo layouts yet. 9. IGMP uni/lib/igmp.icn  covers IGMPv2 (RFC 2236) and IGMPv3 (RFC 3376): PacketSpec   IgmpPacket     Igmpv2Query / Igmpv2Report / Igmpv2Leave     Igmpv3Query / Igmpv3Report Igmp                     session wrapper IGMPv2 messages are an eight-octet header. Report and leave  require()  a group. IGMPv3 queries add flags, QQIC, and a source list. IGMPv3 reports carry group records: ASM join/leave and SSM source filters ( join_sources ,  allow ,  block ). Igmp()  opens  proto=igmp  bound at  0.0.0.0 .  recv(ms)  waits, then returns a table with  kind ,  addr ,  msg ,  len , and decoded fields when recognized.  kind  is one of  v3_report ,  v3_query ,  v2_query ,  v2_report ,  v2_leave , or  unknown . PacketSpec classes work without a raw socket: import net procedure main()    local pkt, v    write(Igmpv2Report().describe())    write(Igmpv3Report().describe())    pkt := Igmpv2Report().report(\"239.1.1.1\")    write(\"v2 report len=\", *pkt)    v := Igmpv2Report().decode(pkt)    write(\"type=\", v[\"type\"], \" group=\", v[\"group\"])    pkt := Igmpv3Report().join(\"239.1.1.1\")    write(\"v3 join len=\", *pkt) end Output: type:int=22  max_resp_time:int=0  checksum:checksum(auto)  group:ipv4(required) type:int=34  reserved1:int=0  checksum:checksum(auto)  reserved2:int=0  nrecords:int=0 v2 report len=8 type=22 group=239.1.1.1 v3 join len=16 Opening a live IGMP socket needs the same privileges as ICMP: import net procedure main()    local g, v    g := Igmp() | stop(&errortext)    write(g.describe())    if v := g.recv(1000) then       write(v[\"kind\"], \" from \", v[\"addr\"])    g.report(\"239.1.1.1\")    # IGMPv2 membership report    g.join(\"239.1.1.1\")      # IGMPv3 ASM join    g.close() end Session send helpers:  report  /  leave  /  query  (v2),  join  /  leave3  (v3 ASM), and  send(bytes)  for a hand-built packet. SSM source-filter joins use the PacketSpec class, not the session wrapper: pkt := Igmpv3Report().join_sources(\"239.1.1.1\", [\"192.0.2.1\"]) g.send(pkt) Exported constants ( IGMP_MEMBERSHIP_QUERY ,  IGMP_V3_MEMBERSHIP_REPORT ,  IGMP_CHANGE_TO_EXCLUDE_MODE , and the rest) are initialized on first  IgmpPacket  construction. 10. Sample programs uni/progs/uping  and  uni/progs/uigmp  are teaching clients, not replacements for  ping(8)  or  tcpdump(1) . They need  SOCK_RAW  privileges: unicon uping unicon uigmp sudo ./uping host [count] sudo ./uigmp [seconds] sudo ./uping -v host    # also show open()/Ping API demos sudo ./uigmp -v         # also show open()/Igmp API demos Default mode uses the session wrappers.  -v  /  -d  also runs the lower-level  open(..., \"nr\", ...)  plus PacketSpec path and prints  describe()  output. sudo ./uping 192.0.2.1 Output: PING 192.0.2.1 (192.0.2.1): 56 data bytes 64 bytes from 192.0.2.1: icmp_seq=0 ttl=118 time=18.390 ms 64 bytes from 192.0.2.1: icmp_seq=1 ttl=118 time=16.245 ms 64 bytes from 192.0.2.1: icmp_seq=2 ttl=118 time=15.533 ms 64 bytes from 192.0.2.1: icmp_seq=3 ttl=118 time=17.311 ms --- 192.0.2.1 ping statistics --- 4 packets transmitted, 4 packets received, 0.0% packet loss round-trip min/avg/max = 15.533/16.869/18.390 ms sudo ./uigmp Output: listening on IGMP 36 bytes from 192.0.2.10: IGMPv3 query group=0.0.0.0 max_resp=100 qrv=2 36 bytes from 192.0.2.10: IGMPv3 query group=239.1.1.1 max_resp=100 qrv=2 36 bytes from 192.0.2.20: IGMPv3 report {239.1.1.1 CHANGE_TO_EXCLUDE} 11. Runtime implementation The work is in the existing POSIX socket code, not a new file type. Mode letter.   src/runtime/fsys.r : after  n  has set  Fs_Socket ,  r  /  R  sets  sock_type = SOCK_T_RAW  and does not set the ordinary read bit. Without  Fs_Socket ,  r  remains  Fs_Read .  e  (crypto raw-key material) is a later check on the same letter;  \"nr\"  is a socket,  \"er\"  is a crypto handle. Create.   src/runtime/rposix.r :  sock_connect  (and the listen/bind path) require  proto=  before  socket() .  uni_getaddrinfo  allows a missing port for  SOCK_T_RAW . The real protocol number is passed as the third argument to  socket() ;  getaddrinfo  is told protocol 0 so it does not override it. I/O.  UDP and raw share  saddrs[] : the  addrinfo  from  open()  is kept until  sock_close() .  sock_write   sendto s there.  sock_recv  accepts  SOCK_DGRAM  and  SOCK_RAW .  proto=  is skipped in  setsockopt  (already consumed).  hdrincl=  sets  IP_HDRINCL . Errors.  Unknown or missing socket attributes use error 1310 with the offending string ( proto ,  proto=nope ,  hdrincl=maybe ). Kernel failures use the system  &errortext . There is no  &features  flag named \"raw sockets\". If POSIX sockets exist, mode  nr  exists. 12. Status and remaining work The language surface described here is implemented:  \"nr\" , required  proto= ,  hdrincl= , datagram  writes  /  receive , PacketSpec, ICMP echo, IGMPv2/v3,  uping , and  uigmp . More PacketSpec layouts.  GRE, OSPF, and PIM names are accepted at  socket()  and the checksum helper documents their exclude ranges, but there are no protocol classes yet. ICMPv6.   proto=icmpv6  opens the socket. Echo, Neighbor Discovery, and an IPv6 header strip are not in  net . IPv6 receive metadata.   strip_ipv4  /  ipv4_ttl  assume a leading IPv4 header. IPv6 raw receives need their own helpers. read()  on raw handles.  UDP clears the read bit so  read()  fails. Raw currently keeps  Fs_Read  because it shares the TCP-like status assignment. The documented API is still  receive() . Aligning the status bits with UDP would make misuse fail earlier. Privileged tests.   tests/posix/raw_sock.icn  checks plumbing without sending packets. A live ICMP echo test would need root in CI and a reachable responder; it is not in the suite. Windows.  Opening  SOCK_RAW  is possible with administrator rights, but Windows historically restricts which protocols a raw socket may use. Treat  uping  /  uigmp  as UNIX-first tools. Header inclusion.   hdrincl=yes  is wired. There is no PacketSpec IPv4 header class to go with it; callers who craft full datagrams build those bytes themselves. Appendix: test coverage tests/posix/raw_sock.icn  does not require  SOCK_RAW  success. It checks that: missing, empty, and unknown  proto=  fail with 1310 proto=icmp ,  gre ,  ospf , and numeric  89  reach  socket()  (success or a privilege denial both print  plumbing-ok ) hdrincl=maybe  is rejected without a preceding  n , mode  \"r\"  still opens a file for reading when  SOCK_RAW  is allowed,  f[\"proto\"]  is 1 after  proto=icmp  and  key(f)  yields populated names (privilege denial still prints  raw-peek:ok  /  raw-key:ok  so the  .std  stays portable) Expected output is  tests/posix/stand/raw_sock.std : missing-proto: bad socket attribute bad-proto: bad socket attribute empty-proto: bad socket attribute raw-icmp: plumbing-ok raw-gre: plumbing-ok raw-ospf: plumbing-ok raw-numeric: plumbing-ok bad-hdrincl: bad socket attribute read-mode-r: ok raw-peek:ok raw-key:ok On an unprivileged account the  raw-*  lines still print  plumbing-ok : the test treats both a successful  socket()  and a privilege denial as passing plumbing. The posix suite picks up every  *.icn  in that directory. References ","title":"UTR 29: Raw Sockets and Packet Layouts in Unicon"}];
